# Member Closure Checklist

Before calling member docs freeze-ready:
- route aliases corrected to real uniapp routes
- component-entry truth called out where route truth does not exist
- member app API docs match real controllers
- missing pages remain planned
- `miniapp.asset.ledger` stays reserved with explicit gate
- errorcopy, KPI, SLA docs use the same active/planned split
