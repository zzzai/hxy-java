# 今日20项工作计划（2026-02-16）

状态说明：
- `[x]` 已完成
- `[-]` 进行中
- `[ ]` 待执行

1. [x] 用户数据与运营模块暂缓策略落盘（暂停清单）
2. [x] 建立本地交互/上下文归档目录
3. [x] 生成本地代码快照目录（时间戳）
4. [x] 导出 `git status` 与最近提交信息
5. [x] 导出工作区 `git diff`（含 binary patch）
6. [x] 导出未跟踪文件清单
7. [x] 复制本轮关键改动文件副本到归档目录
8. [x] 生成代码快照压缩包（tgz）
9. [x] 产出快照索引文档（恢复方式）
10. [x] 新增容器构建忽略规则 `.dockerignore`
11. [x] 新增 `admin` 容器镜像构建文件 `Dockerfile.admin`
12. [x] 新增 `front` 容器镜像构建文件 `Dockerfile.front`
13. [x] 新增后端编排文件 `docker-compose.backend.yml`
14. [x] 新增容器环境模板 `.env.example`
15. [x] 新增容器运维脚本（up/down/build/ps/logs）
16. [x] 新增容器化说明文档 `deploy/docker/README.md`
17. [x] 执行容器编排语法自检（`docker compose config`）
18. [x] 用最小配置完成一次容器冷启动验证（`mysql/redis` 已启动并健康）
19. [x] 补充数据库初始化与迁移执行清单（容器版本）
20. [x] 补充 CI/CD 对接草案（镜像构建/发布/回滚）

## 今日新增产物

1. `hxy/local_archive/2026-02-16_102428`
2. `hxy/local_archive/2026-02-16_102428.tgz`
3. `hxy/local_archive/2026-02-16_103159`
4. `hxy/local_archive/2026-02-16_103159.tgz`
5. `hxy/local_context/*`
6. `crmeb/deploy/docker/*`

## 执行校验记录

1. `docker compose --env-file .env -f docker-compose.backend.yml config`：通过。
2. `docker compose up -d mysql redis`：通过，健康状态 `healthy`。
3. `bash -n deploy/docker/scripts/*.sh`：通过。

## 1+2+3 补充执行记录（当日追加）

1. 新增 RBAC SQL：`hxy/database/database_data_governance_rbac_v1.sql`，补齐 `admin:data:governance:*` 权限点并绑定超管角色。
2. 容器 DB 已执行补丁，数据治理权限菜单生效（`eb_system_menu` 新增 `id=494~505`）。
3. 数据治理联调冒烟全通过：`crmeb/shell/data_governance_smoke.sh`（front+admin 全部 `OK`）。
4. 新增一键发布联调脚本：`crmeb/deploy/docker/scripts/release_smoke.sh`（build/up/health/smoke）。
5. 文档已更新：
   - `crmeb/deploy/docker/README.md`
   - `crmeb/deploy/docker/CICD_BASELINE.md`
6. 一键脚本已验证两种模式：
   - `--skip-build`：通过；
   - 完整 `build + up + smoke`：通过。

## 继续执行（当日增强）

1. 新增容器预检脚本：`crmeb/deploy/docker/scripts/preflight.sh`（依赖/daemon/compose/端口检查）。
2. 新增数据库自动初始化脚本：`crmeb/deploy/docker/scripts/db_init.sh`（可 `--dry-run` / `--force`）。
3. 新增 CI 上线拦截规则聚合脚本：`crmeb/deploy/docker/scripts/ci_gate.sh`（快速/全量模式）。
4. `release_smoke.sh` 集成预检入口（默认先跑 `preflight`，可 `--skip-preflight`）。
5. 文档已补充：
   - `crmeb/deploy/docker/README.md`
   - `crmeb/deploy/docker/CICD_BASELINE.md`
   - `crmeb/deploy/docker/DB_INIT_CHECKLIST.md`
6. 新增 `noexec` 兼容：脚本间调用统一改为 `bash script` 方式，避免直接执行失败。
7. `db_init.sh --force` 已实跑，并完成 Quartz 表治理（最终 `QRTZ_UPPER=10`、`QRTZ_LOWER=0`）。
8. `ci_gate.sh` 快速上线拦截规则已实跑通过（preflight + release_smoke + governance smoke 全绿）。

## 继续执行（CI自动触发 + 管理最佳实践）

1. 已新增 GitHub Actions 自动上线拦截规则：
   - `.github/workflows/crmeb-ci-gate.yml`
2. `ci_gate.sh` 新增 `--init-db` 参数，可在新环境自动初始化数据库后再跑上线拦截规则。
3. `ci_gate.sh --init-db` 已实跑通过（治理冒烟全绿）。
4. 数据治理 RBAC SQL 已补到仓库可跟踪路径：
   - `crmeb/sql/data_governance_rbac_v1.sql`
5. 已补项目管理与开发最佳实践执行清单：
   - `项目管理与开发最佳实践-下一步执行清单.md`
6. K8s 基线目录已搭建：
   - `crmeb/deploy/k8s/base`
   - `crmeb/deploy/k8s/overlays/dev`
   - `crmeb/deploy/k8s/overlays/prod`
   - `crmeb/deploy/k8s/scripts`
7. 当前环境缺少 `kubectl`，K8s 渲染校验未完成（脚本与清单已落盘）。

## 继续执行（4个硬修复逐项落地）

1. 已清理仓库卫生：
   - 清除根 `.gitignore` 冲突标记与无效规则。
   - 清理 `crmeb/.gitignore` 无效项。
2. `db_init` 已完成 strict/replay 双模式：
   - `--mode strict`（CI）
   - `--mode replay`（运维重放）
3. `db_init` 已去除外部绝对路径依赖，SQL 全部收敛到仓库：
   - `crmeb/sql/database_migration_v1.0.sql`
   - `crmeb/sql/database_migration_v2.0.sql`
   - `crmeb/sql/database_architecture_fix_v2.sql`
   - `crmeb/sql/database_compliance_guardrails_v1.sql`
4. `ci_gate` 已支持模式分离：
   - `--db-mode strict|replay`
5. 已新增 K8s 渲染校验 CI（不部署）：
   - `.github/workflows/crmeb-k8s-render.yml`
