# HXY 项目事实基线（v1）

- 日期：2026-03-01
- 用途：作为开发与评审的唯一事实基线（Single Source of Truth）

## 1. 品牌与业务

1. 品牌：荷小悦（新中式·疗愈·社区）。
2. 业务形态：服务项目 + 实物商品 + 套餐组合混合 O2O。
3. 连锁目标：先跑通单店，再复制到 50 店，再区域化扩张。

## 2. 核心业务约束

1. 服务商品核心容量是技师时间，不以库存扣减为核心。
2. 实物商品核心容量是库存，必须具备幂等库存流水。
3. 套餐必须拆子项，按子项履约与退款。
4. 技师提成只针对服务子项，按履约完成计提。

## 3. 组织边界

1. 总部负责：主数据标准、价格边界、活动规则、结算治理。
2. 门店负责：上架、排班、门店价执行、现场履约。
3. 线上配置权限：仅保留总部与门店两级（不设区域配置层）。
4. 线下运行职能：训练、巡检、突发处置可存在，但通过总部工单/台账协同，不形成独立线上配置层。
5. 客服负责：统一售后工单受理、分级、升级与闭环。
6. 供应链负责：补货、调拨、盘点、损耗治理。
7. 财务与审计负责：月结、冲正、四账一致性与审计留痕。

## 4. 交易与支付

1. 支付架构：服务商模式 + 特约商户体系。
2. 资金原则：合规分账、可对账、可审计、可追溯。
3. 退款原则：与履约状态和子项状态绑定。

## 5. 当前工程并轨原则

1. 保留现有 `product_spu/product_sku/trade_order` 主链。
2. 通过增量改造补齐强约束，不做一次性推翻重写。
3. 所有新能力必须可回退。

## 6. 不可破坏项

1. 下单/支付主链稳定性。
2. 退款与对账可追溯性。
3. 多门店隔离与权限边界。
4. 价格与库存幂等一致性。

## 7. 2026-03-02 最新工程事实

1. 门店库存流水已采用抢占执行模型：`PENDING/FAILED -> PROCESSING -> SUCCESS|FAILED`，并带处理租约超时回收。
2. 库存幂等键冲突已强校验：同 `biz_type + biz_no + store_id + sku_id` 不允许出现不同 `incr_count`。
3. 库存业务标识字段不再截断，长度超限直接拒绝，避免幂等键碰撞。
4. SKU 提交链路已补并发幂等兜底：`DuplicateKeyException` 后回查幂等任务返回命中结果，不再返回 500。
5. 长记忆门禁升级为“路径映射 + 内容有效性”强制模式；核心领域改动需同步三份治理文档。
6. 长记忆落地采用 Git 文档事实源，不依赖聊天历史；交接文件统一落 `hxy/07_memory_archive/handoffs/`。
7. 交易下单链路已补模板快照字段透传：`templateVersionId/templateSnapshotJson/priceSourceSnapshotJson` 已打通 `App 下单请求 -> 价格计算 -> trade_order_item`。
8. `trade_order_item` 已补增量脚本 `2026-03-02-hxy-trade-order-item-template-snapshot.sql`，用于模板/价格来源快照落库。
9. 服务预约网关当前生效实现为 `com.hxy.server.config.booking.ServerTradeServiceBookingGateway`（按 `payOrderId` 对齐 booking 订单并幂等标记 `BOOKED`）。
10. 价格计算阶段已前置服务/实物分流强约束：`deliveryType=EXPRESS` 且命中服务商品时，直接拒绝下单计算。
11. 价格计算阶段已补模板版本强校验（当请求携带 `templateVersionId`）：若请求缺少 `templateSnapshotJson`，服务端优先使用模板版本 `snapshotJson` 自动回填；若仍为空则拒绝下单计算；并校验模板版本“存在、已发布、类目匹配 SPU”。
12. 交易域模板校验已改为通过 Product API (`ProductTemplateVersionApi`) 读取模板版本，避免跨域直接访问 DAL。
13. 价格计算阶段已补“服务商品模板版本必填”约束：命中 `service` 商品但缺失 `templateVersionId` 时，直接拒绝下单计算。
14. 商品主数据已补 `SPU/SKU.templateVersionId` 字段与迁移脚本：`product_spu`、`product_sku` 均可显式绑定模板版本，服务商品保存时强校验“存在、已发布、类目匹配”。
15. 服务商品模板版本读取已支持主数据兜底：下单请求未传 `templateVersionId` 时，价格计算阶段按 `SKU -> SPU` 顺序回填，并继续执行发布态与快照校验。
16. 预约网关已实接占位创建：`ServerTradeServiceBookingGateway` 查不到 `payOrderId` 对应 booking 时，自动调用 `BookingOrderService.createPlaceholderOrder` 并回写服务履约单 `markBooked`。
17. 预约模块测试基线已补齐 `booking_order.dispatch_mode/parent_order_id/is_addon/addon_type` 字段，确保占位链路单测与 H2 结构一致。
18. 支付 P0 收口 CI 已新增 `hxy-payment-stagea-p0-23`：默认执行命名门禁、记忆门禁、模板绑定单测、交易模板回填单测、预约占位单测、网关联调单测。
19. 套餐售后退款上限已改为“子项履约优先”：当快照存在 `bundleChildren` 时，按子项履约/可退状态汇总可退上限；仅在无有效子项结果时才回退 `bundleRefundablePrice` 显式值。
20. 售后退款上限审计明细已增强：统一记录 `snapshotField/serviceSnapshotField`，并固化 `bundleChildren` 证据（`childCode/refundCapPrice/fulfilled/refundable/included`）。
21. 售后退款守卫已补强：当存在服务履约单且状态为 `FINISHED` 时，退款上限直接收敛为 0（即使履约快照缺失），避免因脏数据回退到订单项实付金额而放行退款。
22. 管理后台售后分页已支持按 `refundLimitSource` 精确筛选，便于运营定位不同限退口径（`SERVICE_ORDER_SNAPSHOT/ORDER_ITEM_PRICE_SOURCE/ORDER_ITEM_PAY_PRICE`）的异常单。
23. 管理后台售后详情已返回结构化退款上限审计字段 `refundLimitDetail`：后端将 `refundLimitDetailJson` 安全解析为对象；当 JSON 非法或为空时返回 `null`，避免前端重复解析与异常分支。
24. 管理后台售后详情已补 `refundLimitSourceLabel/refundLimitRuleHint`：后端根据 `refundLimitSource` 输出可读标签与规则说明，减少前端枚举映射与文案维护成本。
25. 售后统一工单分页已补 SLA 可检索维度：新增 `overdue`（仅待处理状态按 `slaDeadlineTime` 判定）与 `lastActionTime` 时间窗筛选。
26. 售后统一工单已对齐审计字段：新增并落库 `lastActionCode/lastActionBizNo/lastActionTime`，覆盖创建、规则再触发、SLA 自动升级、人工/自动收口全动作。
27. 售后统一工单分页已支持审计动作维度筛选：`lastActionCode/lastActionBizNo` 可直接检索，支撑客服与风控按动作追踪异常闭环。
28. 佣金结算审批状态机已对齐为 `草稿(0) -> 待审(10) -> 通过(20)/驳回(30)/作废(40)`，SLA 超时回收统一落 `40=作废`。
29. 结算状态守卫已补单测：非法状态提审会被拒绝；超时回收用例断言目标状态为 `VOIDED(40)`。
30. 提成冲正已落幂等键三元组：`biz_type + biz_no + staff_id`，并补唯一索引 `uk_reversal_idempotent`（SQL 与测试建表同步）。
31. 重复取消请求已按幂等键收敛：已存在冲正记录直接幂等返回；已取消冲正记录按同幂等键回激活为待结算并清空结算绑定。
32. 提成冲正幂等命中已补“负载一致性强校验”：同幂等键下若 `base_amount/commission_rate/commission_amount` 与预期不一致，直接抛业务异常 `COMMISSION_REVERSAL_IDEMPOTENT_CONFLICT`，禁止重复落账。
33. 结算审批 SLA 升级链路已改为状态迁移：超时记录从 `PENDING_REVIEW(10)` 自动迁移至 `ESCALATED(15)`，并落库 `review_escalate_time/review_escalate_reason=SLA_OVERDUE_ESCALATE_P0`。
34. 结算审批守卫已兼容升级态：`approve/reject` 允许从 `PENDING_REVIEW(10)` 或 `ESCALATED(15)` 进入终态，避免升级后单据无法继续审批。
35. 提成冲正新增 `origin_commission_id` 字段，并新增唯一索引 `uk_origin_commission_id`（仅非空生效），用于约束“同原佣金最多一条有效冲正”。
36. `cancelCommission` 创建冲正时已写入 `origin_commission_id`；命中唯一索引冲突（`DuplicateKeyException`）会回查既有冲正并按幂等成功处理，不重复落账。
37. 已取消冲正记录在重新取消时会先释放幂等键（`origin/biz/staff` 清空）再新建有效冲正，保留取消历史且允许重新生成。
38. 售后统一工单 SLA 路由已从硬编码升级为可配置解析器：新增 `AfterSaleReviewTicketRouteProvider` 抽象与默认实现 `DefaultAfterSaleReviewTicketRouteProvider`。
39. 工单路由决策顺序已固定为 `ruleCode -> ticketType+severity -> ticketType 默认 -> 全局默认`，并在创建、规则再触发、SLA 自动升级链路统一生效。
40. 售后工单 SLA 路由规则配置中心已落地：新增表 `trade_after_sale_review_ticket_route`（含默认种子规则）与后台管理 API（创建/更新/删除/详情/分页/启用列表）。
41. 路由解析已切到“DB 配置优先 + 内置规则兜底”：默认实现按 30 秒本地缓存读取启用规则，配置变更后通过 `invalidateCache` 立即失效刷新。
42. 路由配置写入守卫已启用：按 `scope + ruleCode + ticketType + severity` 唯一键防冲突，并在服务层执行作用域字段合法性强校验。
43. 门店生命周期停业/闭店守卫已补强：除 `PENDING/PROCESSING` 外，若存在 `FAILED` 库存流水也阻断状态迁移，避免在库存异常未闭环时提前停业或闭店。
44. 套餐售后已补“退款执行前二次校验”：`refundAfterSale/processRefundAfterSale` 会按最新履约快照重算可退上限并回写审计明细；若上限收紧且当前退款额超限，除拦截并返回 `AFTER_SALE_REFUND_FAIL_REFUND_LIMIT_CHANGED` 外，还会落 `REFUND_LIMIT_CHANGED` 风控审计并触发工单中台收口。
45. 门店生命周期守卫已接入交易域统计：通过 `TradeStoreLifecycleGuardApi` 聚合 `pendingOrderCount/inflightTicketCount`，并按 `infra_config` 五项开关（`mapping/stock/stock-flow/pending-order/inflight-ticket`）执行 `WARN/BLOCK` 判定；`WARN` 命中写审计原因 `LIFECYCLE_GUARD_WARN:<guardKey>:count=<n>`。
46. 售后工单 SLA 路由已完成“去代码映射”收口：`DefaultAfterSaleReviewTicketRouteProvider` 仅使用 DB 启用规则按优先级解析（`rule -> type+severity -> type -> global`），不再回退内置 `RULE/TYPE` 映射；仅保留全局默认兜底 `P1/HQ_AFTER_SALE/120`。
47. 套餐子项退款快照已新增显式字段 `bundleItemSnapshotJson`（固化于服务履约单 `orderItemSnapshotJson`）；售后退款上限计算优先读取该字段，再回退 `bundleRefundSnapshotJson/priceSourceSnapshotJson`，并在服务完成态同步冻结两个快照字段，降低对 `priceSource` 原始结构的耦合。
48. 门店生命周期守卫已新增详情接口 `GET /product/store/lifecycle-guard?id=<storeId>&lifecycleStatus=<target>`：返回统一评估结果 `blocked/blockedCode/blockedMessage/warnings/guardItems`，并与 `saveStore/updateStoreLifecycle` 复用同一守卫评估器，确保“预检查口径 = 实际拦截口径”。
49. 门店生命周期守卫已新增批量预检接口 `POST /product/store/lifecycle-guard/batch`：按去重后的门店列表返回逐店守卫结果，支持批量停业/闭店前一次性看阻塞项与 WARN 项。
50. 门店生命周期已新增批量执行汇总接口 `POST /product/store/batch/lifecycle/execute`：按去重门店“先守卫预检、再执行迁移”，阻塞门店直接跳过，返回 `total/success/blocked/warn` 汇总与逐店执行明细（含 `blockedCode/blockedMessage/warnings`）及审计摘要 `LIFECYCLE_BATCH_EXEC:*`。
51. `trade_order_item` 已新增显式字段 `bundle_item_snapshot_json`：下单建单时从 `priceSourceSnapshotJson` 提取并固化套餐子项快照；售后退款上限计算在无服务履约快照时，优先读取该字段，再回退 `priceSourceSnapshotJson`，并将命中字段写入审计 `snapshotField`。
52. 交易价格来源快照已统一由服务端生成并落单：决策顺序固定为 `HEADQUARTER > STORE > ACTIVITY > BENEFIT`，统一输出 `baseSource/headquarterPrice/storePrice/basePrice/activityDiscountPrice/couponPrice/pointPrice/vipPrice/payPrice/promotionHits`；若请求已携带快照对象（如套餐子项证据），会在原对象上合并字段而不是覆盖。
53. 预约域与交易履约域已补状态同步 API：`BookingOrderServiceImpl` 在 `startService/completeService/cancelOrder(PAID)/refundOrder/updateOrderRefunded` 后，按 `payOrderId` 调用 `TradeServiceOrderApi` 批量同步 `BOOKED->SERVING->FINISHED/CANCELLED`；同步失败降级为告警日志，不阻断预约主流程。
54. 售后工单 SLA 路由中心已补运维收口接口：新增批量启停 `POST /trade/after-sale/review-ticket-route/batch-update-enabled`、批量删除 `POST /trade/after-sale/review-ticket-route/batch-delete` 与命中预览 `GET /trade/after-sale/review-ticket-route/resolve`；命中顺序固定 `RULE > TYPE_SEVERITY > TYPE_DEFAULT > GLOBAL_DEFAULT`，无配置时兜底 `GLOBAL_DEFAULT_FALLBACK(P1/HQ_AFTER_SALE/120)`。
55. 售后工单已新增“路由命中留痕”快照字段：`trade_after_sale_review_ticket` 落库 `route_id/route_scope/route_decision_order`，在工单创建、规则再触发、SLA 自动升级三条写链路同步更新；路由提供器返回值补齐命中元数据（DB 规则命中写真实 `route_id/scope`，兜底写 `GLOBAL_DEFAULT_FALLBACK`）。
