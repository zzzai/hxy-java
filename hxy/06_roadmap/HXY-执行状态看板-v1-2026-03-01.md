# HXY 执行状态看板（v1）

- 日期：2026-03-01
- 口径：Done / Partial / Todo

## 1. 商品与门店主链

1. 服务商品与实物商品管理入口分离：Done
2. 门店主数据（分类/标签/标签组/生命周期）：Done
3. 门店 SKU 批量铺货、批量调价/调库存：Done
4. 套餐子项模型（交易主链级）：Done（已新增 `hxy_trade_order_item_bundle_child` 台账并打通建单幂等落库、履约状态同步、退款回写）
5. PRD 场景补齐（规则引擎/合约策略/快照冻结/分层库存）：Done
6. PRD 场景补齐（角色RACI/结算SLA/工单/供应链/生命周期）：Done

## 2. 模板与SKU生成

1. 类目模板版本化与模板项：Done
2. 模板校验 API：Done
3. SKU 预览与提交（含幂等与失败重试）：Done
4. 订单项落模板快照：Done（已落库字段与链路透传，已补“存在/已发布/类目匹配”校验、服务端自动回填模板快照、服务商品 `templateVersionId` 必填、`SPU/SKU` 主数据显式绑定）

## 3. 交易与履约

1. 服务履约单骨架（支付后建单）：Done
2. 服务预约网关对接真实预约域：Done（`ServerTradeServiceBookingGateway` 已在 booking 缺失时主动创建占位预约并回写服务履约单）
3. 服务与实物分流强校验（按 productType）：Done（价格计算与发货阶段双门禁）
4. 套餐拆子项履约与退款：Done（已完成“子项台账优先 + 快照回退”双轨上限、退款执行前二次复核、已履约子项阻断与 `refunded_price` 回写）

## 4. 价格与库存

1. 门店价覆盖能力：Done
2. 总部价/门店价/活动价统一决策器：Done（交易服务端统一生成 `priceSourceSnapshotJson`，固定顺序 `HEADQUARTER->STORE->ACTIVITY->BENEFIT`，并保留上游证据字段）
3. 幂等库存流水唯一键：Done
4. 库存补偿重试机制：Done

## 5. 财务与提成

1. 支付主链与退款联调：Partial
2. 技师提成计提（履约完成）：Partial（booking 侧基础计提已落，需继续与交易/售后链路收口）
3. 退款冲正提成：Partial（已补冲正幂等键、`origin_commission_id` 唯一约束、重复请求幂等返回、同键不同金额冲突拦截）
4. 四账对账（交易/履约/提成/分账）：Done（已落地日快照 + 手工触发 + 日任务 + `page/get/summary` + WARN 建单 + PASS 收口 + 关联工单联查）
5. 结算审批流与SLA落系统：Done（审批状态机、SLA 预警/升级、通知出站重试、运营检索与审计字段均已收口）

## 6. 客服与供应链

1. 售后统一工单中台：Done（已收口统一工单创建/升级/收口、分页审计检索、通知出站、批量处置与 overlay 运营页）
2. 工单SLA与升级规则：Done（规则中心 `trade_ticket_sla_rule` + 管理 API + 命中预览 + overlay 路由配置页已落地）
3. 补货/调拨/盘点/损耗闭环：Done（库存调整单 + 跨店调拨审批闭环已落地，盘点差异稽核工单自动联动并支持 fail-open）
4. 门店生命周期流程引擎：Done（变更单审批化、批次台账、复核台账、SLA 超时收口与 overlay 运营页均已落地）

## 7. 证据与门禁

1. 命名门禁：Done
2. 模板生成链路脚本：Done
3. 长记忆门禁：Done
4. 长记忆门禁接入 CI：Done
5. 支付 P0-23 专项 CI（模板绑定 + 预约占位 + 门禁）：Done

## 9. 2026-03-02 增量

1. 库存并发一致性修复：Done（新增 `PROCESSING` 抢占执行与租约超时回收）。
2. 库存幂等一致性修复：Done（幂等键冲突与业务标识长度超限强校验）。
3. SKU 提交幂等并发修复：Done（`DuplicateKeyException` 兜底回查）。
4. 长记忆索引与交接协议：Done（`memory-index.yaml` + `handoffs` 模板）。
5. 订单项模板快照字段链路打通：Done（请求/计算/落库三段透传 + 单测）。
6. 模板版本发布态强校验前置：Done（价格计算阶段校验快照必填、版本存在、状态已发布、类目匹配）。
7. 服务商品快递下单前置拦截：Done（价格计算阶段直接拒绝 `service + express` 组合）。
8. 模板快照服务端自动回填：Done（当请求携带 `templateVersionId` 且快照缺失时，优先回填模板版本 `snapshotJson`，空快照仍阻断）。
9. 服务商品模板版本必填：Done（价格计算阶段，`service` 商品缺少 `templateVersionId` 直接拒绝）。
10. SPU/SKU 模板版本主数据绑定：Done（`product_spu/product_sku.template_version_id` + 保存态强校验 + 交易侧主数据兜底读取）。
11. 预约占位实接：Done（booking 服务新增 `createPlaceholderOrder` 幂等接口，网关自动占位并回写 `markBooked`）。
12. 支付 CI 收口：Done（新增 `run_payment_stagea_p0_23.sh`、`payment-stagea-p0-23.yml`，并接入 required checks 配置脚本）。
13. 套餐售后退款上限与审计增强：Done（优先按 `bundleChildren` 计算可退上限；审计 detail 固化 `snapshotField/serviceSnapshotField` 与子项证据明细）。
14. 服务履约完成态退款阻断：Done（存在 `serviceOrder=FINISHED` 时即使快照缺失也阻断退款，避免回退到订单项实付口径）。
15. 售后分页限退来源筛选：Done（管理后台分页新增 `refundLimitSource` 条件，支持按口径快速排查）。
16. 售后详情结构化审计明细：Done（新增 `refundLimitDetail` 字段，后端安全解析 `refundLimitDetailJson`，非法 JSON 自动降级 `null`）。
17. 售后详情来源标签与规则说明：Done（新增 `refundLimitSourceLabel/refundLimitRuleHint`，后端统一映射退款上限来源展示口径）。
18. 售后统一工单 SLA/审计增强：Done（分页新增 `overdue + lastActionCode/lastActionBizNo/lastActionTime` 检索；工单新增 `lastAction*` 审计三元组并覆盖创建/再触发/升级/收口动作）。
19. 结算审批状态机口径统一：Done（`40` 状态统一为 `作废/VOIDED`，SLA 超时回收落作废态）。
20. 提成冲正幂等键落库：Done（`biz_type + biz_no + staff_id` + 唯一索引 `uk_reversal_idempotent`）。
21. SLA Job 单测补强：Done（新增非法参数回退与上限钳制用例，覆盖预警/升级参数守卫）。
22. 提成冲正幂等冲突强校验：Done（同幂等键下金额/比例不一致直接报错 `COMMISSION_REVERSAL_IDEMPOTENT_CONFLICT`）。
23. 结算 SLA 升级状态迁移：Done（超时待审自动由 `PENDING_REVIEW(10)` 迁移到 `ESCALATED(15)`，并记录升级时间与原因）。
24. P1 定向单测补齐：Done（覆盖冲正幂等命中、同键不同金额拦截、SLA 升级触发与并发跳过场景）。
25. 冲正 origin 唯一幂等收口：Done（新增 `origin_commission_id` 字段与 `uk_origin_commission_id`，创建冲正写 origin，唯一冲突按幂等命中处理）。
26. 取消态冲正再生成策略：Done（已取消冲正先释放幂等键再新建有效冲正，保持取消历史并避免重复落账）。
27. 售后工单 SLA 路由规则化：Done（新增 `AfterSaleReviewTicketRouteProvider` 与默认实现；创建/再触发/自动升级统一按 `ruleCode -> ticketType+severity -> ticketType -> default` 解析）。
28. 售后工单 SLA 路由配置中心化：Done（新增 `trade_after_sale_review_ticket_route` 表与默认规则种子、后台管理 API、解析器 DB 优先 + 兜底策略、配置变更缓存失效刷新）。
29. 门店生命周期守卫收口：Done（停业/闭店阻塞条件扩展为 `PENDING/FAILED/PROCESSING` 库存流水，避免库存异常未闭环即迁移生命周期）。
30. 套餐售后退款执行守卫：Done（退款执行前按最新履约快照重算上限并回写审计；上限收紧导致超限时阻断退款，错误码 `AFTER_SALE_REFUND_FAIL_REFUND_LIMIT_CHANGED`，并落 `REFUND_LIMIT_CHANGED` 风控审计/工单）。
31. 门店生命周期守卫交易域补齐：Done（新增 `TradeStoreLifecycleGuardApi` 聚合未结订单/在途售后；`product` 侧按 `infra_config` 五项守卫键执行 `WARN/BLOCK`，`WARN` 统一写审计原因）。
32. 工单 SLA 路由配置中心化后端收口：Done（移除 provider 内置 `RULE/TYPE` 代码映射，解析仅依赖 DB 规则 + 全局默认兜底，配置口径与运行口径一致）。
33. 套餐子项快照显式字段收口：Done（服务履约单快照新增 `bundleItemSnapshotJson`；售后上限解析优先读取该字段并兼容旧字段；服务完成时同步冻结 `bundleItemSnapshotJson/bundleRefundSnapshotJson`）。
34. 门店生命周期守卫详情接口：Done（新增 `GET /product/store/lifecycle-guard` 返回 `blocked/warnings/guardItems`；预检与执行复用同一守卫评估器，避免口径不一致）。
35. 门店生命周期守卫批量预检接口：Done（新增 `POST /product/store/lifecycle-guard/batch`，按去重门店列表返回逐店守卫结果，支撑批量停业/闭店前统一预检）。
36. 门店生命周期批量执行汇总接口：Done（新增 `POST /product/store/batch/lifecycle/execute`，逐店复用守卫评估后执行生命周期迁移，阻塞项跳过并返回 `total/success/blocked/warn` 与逐店明细审计摘要）。
37. 套餐子项可退快照主表化：Done（`trade_order_item` 新增 `bundle_item_snapshot_json`，建单阶段提取并固化；售后无服务快照时优先读取该字段并回写 `snapshotField=bundleItemSnapshotJson` 审计来源）。
38. 价格来源快照统一收口：Done（`TradePriceServiceImpl` 在计算完成后统一生成快照并合并保留原有对象型快照，补齐 `baseSource/basePrice/payPrice/promotionHits` 等审计字段）。
39. 预约->交易履约状态同步：Done（新增 `TradeServiceOrderApi`，预约在开单/完单/取消/退款及退款回调后按 `payOrderId` 批量同步交易履约单状态，失败降级告警不阻断主流程）。
40. 售后路由中心运维收口：Done（新增批量启停、批量删除、命中预览三接口；规则命中顺序与工单创建链路保持一致，支撑运营批量治理与配置前排障）。
41. 工单路由命中审计快照：Done（工单主表新增 `route_id/route_scope/route_decision_order`，并在创建/再触发/SLA 升级链路同步写入，管理端详情可直接读取命中来源）。
42. 工单路由维度分页筛选：Done（分页请求/SQL 已支持 `routeId/routeScope` 精确检索，配合路由配置中心支持规则命中工单回查）。
43. 工单分页 `routeScope` 入参标准化：Done（服务层查询前统一 `trim + uppercase`，降低大小写/空白造成的筛选漏单）。
44. 工单分页筛选参数规范化扩展：Done（`severity/ruleCode/escalateTo/lastActionCode` 统一大写规范化，`sourceBizNo/lastActionBizNo` 统一 trim，空白值归一为 null）。
45. 工单分页索引补强：Done（新增 `idx_route_scope_id`、`idx_status_create_time`、`idx_last_action_time`，覆盖路由与时间维度高频筛选）。
46. 工单批量收口接口：Done（新增 `batch-resolve`，按“存在且待处理”守卫执行，返回成功/不存在/非待处理三类汇总及明细 ID）。
47. 工单管理端最小页接入：Done（overlay 新增 reviewTicket 页面与 API，支持分页筛选、详情查看、单条收口；菜单 SQL 已补齐）。
48. 工单批量收口审计号唯一化：Done（批量收口自动生成 `resolveBizNo#ticketId`，提升多单并发收口的审计可追溯性）。
49. 工单批量收口管理端收口：Done（工单页新增勾选批量收口入口，提交前提示非待处理跳过，执行后展示 `success/notFound/notPending` 汇总回执）。
50. 门店库存人工调整入口：Done（新增 `manual-stock-adjust`，标准化补货/调拨/盘点/损耗业务类型，并强校验方向与重复 SKU，统一接入库存流水幂等链路）。
51. 门店库存流水分页接口：Done（新增 `stock-flow/page`，支持多维检索与门店名称展示，补齐供应链操作后的审计查询能力）。
52. 门店 SKU 管理端库存运营页：Done（overlay 门店 SKU 页新增人工库存调整弹窗与库存流水台账弹窗，支持前置方向校验与多维分页筛选）。
53. 门店库存流水批量重试闭环：Done（新增 `stock-flow/batch-retry` 接口与管理端勾选重试按钮，仅重试 `待执行/失败` 流水并返回成功触发数量）。
54. 服务商品库存禁入守卫：Done（库存增减执行前按 SPU 类型校验，服务商品直接拒绝进入库存链路，满足“服务/实物分流”约束）。
55. 门店库存批量重试结果可观测性：Done（`batch-retry` 返回结构化明细：成功/跳过/失败及原因，管理端新增结果弹窗与汇总展示）。
56. 门店库存重试审计展示：Done（库存流水新增 `lastRetryOperator/lastRetrySource` 字段，后端写入操作人/来源，管理端台账新增列展示）。
57. 门店台账失败项快捷处置：Done（新增“一键勾选失败项”按钮，按 `status=FAILED` 自动勾选当前页流水后批量重试）。
58. 门店生命周期库存守卫增强：Done（停业/闭店前库存判定改为“非零库存阻塞”，覆盖负库存异常场景，避免异常库存状态下迁移生命周期）。
59. 门店生命周期库存流水分项可视化：Done（`guardItems` 新增 `stock-flow-pending/processing/failed` 计数明细，并与 `stock-flow` 总阻塞项联动，便于预检定位阻塞来源）。
60. SLA 工单规则中心化（trade+booking）：Done（新增 `trade_ticket_sla_rule` 与管理 API，规则命中优先级固定 `RULE > TYPE_SEVERITY > TYPE_DEFAULT > GLOBAL_DEFAULT`）。
61. trade 工单路由规则中心接入：Done（人工复核工单创建/再触发/自动升级统一走规则 API，保留兜底路由避免中心故障影响主流程）。
62. booking 结算提审 SLA 规则化：Done（提审默认 SLA 从规则中心读取，未命中回退常量兜底）。
63. 门店生命周期库存流水守卫分层配置：Done（`pending/processing/failed` 分状态 `WARN/BLOCK` 配置已落地，兼容回退旧键 `stock-flow.mode`；新增 SQL 种子与单测覆盖“仅失败流水 WARN 不阻塞”场景）。
64. 售后工单路由兜底收口：Done（移除 `ruleCode` 硬编码 fallback 映射，统一 `GLOBAL_DEFAULT_FALLBACK` 口径；规则中心异常时按严重级别兜底并保持审计字段一致）。
65. 售后工单 SLA 运行参数配置化：Done（升级任务批量阈值与 fallback 路由参数改为 `infra_config` 在线可调，代码保留上限守卫并补齐单测）。
66. 售后工单升级延迟窗口收口：Done（自动升级链路接入 `escalateDelayMinutes`，未到延迟窗口的逾期工单本轮跳过，避免提前升级）。
67. 售后工单升级批量上限一致性修复：Done（job/service/mapper 批量上限统一到 5000，修复配置提升后仍被服务层 1000 截断的问题）。
68. 售后工单分页收口审计筛选增强：Done（新增 `resolverId/resolverType/resolveActionCode/resolveBizNo` 检索维度，并补 mapper/service 单测覆盖）。
69. 售后工单 SLA 预警通知出站闭环：Done（新增 `trade_after_sale_review_ticket_notify_outbox` + 服务层预警入箱 `SLA_WARN`、升级入箱 `SLA_ESCALATE`，幂等键防重）。
70. 售后工单通知分发与失败补偿：Done（新增 `dispatchPendingNotifyOutbox`，成功回写 `SENT`，失败回写 `FAILED + retryCount + nextRetryTime` 并保留 `lastErrorMsg/lastAction*`）。
71. 售后工单通知运营接口：Done（新增 `notify-outbox-page` 分页检索与 `notify-outbox-batch-retry` 批量重试，返回结构化成功/跳过明细）。
72. 售后工单 SLA 任务补齐：Done（新增 `AfterSaleReviewTicketWarnJob` 与 `AfterSaleReviewTicketNotifyDispatchJob`，批量阈值支持配置化，补齐 Job 单测）。
73. 门店生命周期批量执行台账：Done（新增 `hxy_store_lifecycle_batch_log`、生命周期批量执行落库与分页检索接口，补齐 Controller/Service/Mapper 单测）。
74. 四账对账最小闭环：Done（新增 `hxy_four_account_reconcile`、`page/run` 管理接口与 `FourAccountReconcileDailyJob`，并补告警标签与并发唯一键覆盖更新逻辑）。
75. 门店生命周期预检状态机收口：Done（`getLifecycleGuard/getLifecycleGuardBatch` 新增生命周期流转合法性校验，预检阶段直接回传 `STORE_LIFECYCLE_TRANSITION_NOT_ALLOWED`，避免“预检通过但执行失败”）。
76. 门店生命周期守卫执行顺序收口：Done（预检改为“状态机优先、资源守卫其次”；非法流转场景不再执行库存/在途单据守卫查询，阻塞原因稳定为状态机错误）。
77. 四账告警自动建工单闭环：Done（booking 对账快照在 `WARN` 场景调用 `TradeReviewTicketApi.upsertReviewTicket`，按 `FOUR_ACCOUNT_RECONCILE:<bizDate>` 幂等汇聚到同一工单）。
78. 统一工单来源幂等键收口：Done（trade 服务新增 `upsertReviewTicketBySourceBizNo`，并新增 SQL `2026-03-05-hxy-review-ticket-idempotent-source.sql` 与测试建表唯一键，保障 `ticketType + sourceBizNo` 并发幂等）。
79. 四账通过自动收口闭环：Done（booking 在 `PASS` 场景调用 `TradeReviewTicketApi.resolveReviewTicketBySourceBizNo`，按同一来源号自动收口待处理工单，动作码 `FOUR_ACCOUNT_RECONCILE_PASS`）。
80. 统一工单按来源收口能力：Done（trade 服务新增 `resolveReviewTicketBySourceBizNo`，仅 `PENDING` 状态允许收口，终态或未命中按 no-op 处理）。
81. 四账台账联查工单摘要：Done（trade-api 新增批量摘要查询接口，booking 四账分页回填 `relatedTicketId/status/severity`，overlay 台账增加关联工单可视字段）。
82. 门店库存流水审计检索与明细对齐：Done（`stock-flow/page` 新增 `operator/source` 筛选并做参数规范化；`stock-flow/batch-retry` 明细新增 `storeId/skuId`，管理端可直接展示失败项归属）。
83. 四账台账详情与联查降级收口：Done（新增 `GET /booking/four-account-reconcile/get`，`/page` 与 `/get` 统一回填 `sourceBizNo`；trade 摘要联查异常按 fail-open 降级不阻断；trade 摘要 DTO 新增 `resolvedTime` 并补测试）。
84. 多窗口协作总控机制：Done（窗口 A 固化为 CTO/集成窗口，A/B 当前锁定分支 `feat/ui-four-account-reconcile-ops`；新增协作治理文档并纳入 memory-index，统一后续窗口接入、交付与门禁口径）。
85. 门店生命周期守卫复盘能力增强：Done（新增 `GET /product/store/lifecycle-batch-log/get` 与 `POST /product/store/lifecycle-guard/recheck-by-batch`；批次日志新增 `guardRuleVersion/guardConfigSnapshotJson`；`detailJson` 非法时降级返回 `detailParseError`，不阻断查询与复核）。
86. 门店生命周期复核执行台账：Done（新增 `POST /product/store/lifecycle-guard/recheck-by-batch/execute` 落复核台账；新增 `hxy_store_lifecycle_recheck_log` 与 `GET /product/store/lifecycle-recheck-log/page|get`；复核详情 `detailJson` 非法时降级解析并回传 `detailParseError`）。
87. 门店生命周期审批化最小闭环：Done（新增 `hxy_store_lifecycle_change_order` 与接口 `create/submit/approve/reject/cancel/get/page`；状态机强约束为 `DRAFT->PENDING->APPROVED/REJECTED/CANCELLED`；`approve` 二次复核守卫通过后才调用 `updateStoreLifecycle` 执行迁移，`reject/cancel` 不改门店生命周期）。
88. 门店生命周期审批运营化增强：Done（变更单新增 `submitTime/slaDeadlineTime/lastAction*` 审计字段，分页新增 `overdue/lastActionCode/lastActionOperator` 筛选；新增 `ProductStoreLifecycleChangeOrderExpireJob` 对超时待审单自动收口 `CANCELLED`，动作码 `EXPIRE`，且不触发生命周期迁移）。
89. 门店 SKU 库存调整单审批闭环：Done（新增 `hxy_store_sku_stock_adjust_order` 与接口 `create/submit/approve/reject/cancel/get/page`；审批通过复用既有库存幂等流水执行并固化 `lastAction*` 审计，覆盖补货/调拨/盘点/损耗六类业务）。
90. 门店 SKU 跨店调拨审批闭环：Done（新增 `hxy_store_sku_transfer_order` 与接口 `create/submit/approve/reject/cancel/get/page`；审批通过同步执行源店出库 + 目标店入库，流水 `bizNo=orderNo` 幂等收敛）。
91. 盘点差异稽核工单自动联动：Done（库存调整单 `STOCKTAKE` 审批通过时按阈值触发 `TradeReviewTicketApi.upsertReviewTicket`；配置化开关/阈值 `product.stocktake.audit-ticket.enabled`、`product.stocktake.audit-threshold`；工单异常 fail-open）。
92. 四账对账汇总接口运营化：Done（新增 `GET /booking/four-account-reconcile/summary`，支持 `日期区间 + 状态 + 是否已关联工单` 聚合返回，总笔数/通过告警数/差异金额聚合/未收口工单数）。
93. 四账汇总跨域降级策略：Done（trade 工单摘要异常时返回降级结果并标记 `ticketSummaryDegraded=true`，不阻断 booking 对账主链路查询）。
94. 库存门禁 StageB 扩展：Done（`check_store_sku_stock_gate.sh` 新增库存调整单/跨店调拨单待审批超时检查，统一 `summary+tsv` 输出并固化 `SS01~SS15` 错误码，支持 `BLOCK/WARN` 分级）。
95. 生命周期审批门禁新增：Done（新增 `check_store_lifecycle_change_order_gate.sh`，检查 `hxy_store_lifecycle_change_order` 待审批超时与 EXPIRE 收口一致性，统一 `summary+tsv` 输出并固化 `LC01~LC07` 错误码）。
96. StageB/P1 本地一键验收脚本：Done（新增 `run_ops_stageb_p1_local_ci.sh`，串联命名门禁、记忆门禁、库存门禁、生命周期门禁与 `product/trade/booking` 关键回归测试，支持 DB 参数与 `--skip-tests/--skip-mysql-init` 分段跳过）。
97. StageB/P1 CI 收口与 required checks 接入：Done（新增 workflow `.github/workflows/ops-stageb-p1-guard.yml`，支持 `workflow_dispatch + 关键路径触发` 并上传 `summary/tsv` 构件；`setup_github_required_checks.sh` 新增 `INCLUDE_OPS_STAGEB_CHECKS=1` 可选上下文，默认不破坏 stagea 检查集）。
98. 套餐子项交易主链表化与退款一致性闭环：Done（新增 `hxy_trade_order_item_bundle_child` 与 trade 子项 DO/Mapper；建单阶段落子项台账并按 `order_item_id` 幂等；售后创建/退款执行上限计算优先走子项台账、缺失回退快照并落 `CHILD_LEDGER/FALLBACK_SNAPSHOT` 审计；执行前二次复核命中已履约子项返回 `AFTER_SALE_REFUND_FAIL_BUNDLE_CHILD_FULFILLED`，退款成功后按子项余额回写 `refunded_price`；补齐 mapper/建单/履约回写/售后校验单测）。
99. StageB required checks 运维闭环：Done（`setup_github_required_checks.sh` 已支持 `--dry-run/--apply + owner/repo 自动解析 + stagea/stageb profile`；新增 `apply_ops_stageb_required_checks.sh` 与 `rollback_ops_stageb_required_checks.sh`，执行前打印 checks 清单、执行后输出分支保护摘要，形成可一键启用/可回滚/可审计流程）。
100. 退款-提成联调巡检接口：Done（新增 `GET /booking/four-account-reconcile/refund-commission-audit-page`，按支付时间窗口聚合 `trade_order + technician_commission`，输出 `REFUND_WITHOUT_REVERSAL/REVERSAL_WITHOUT_REFUND/REVERSAL_AMOUNT_MISMATCH` 三类异常与订单级审计证据，支持分页筛选与运营回查）。
101. 退款-提成巡检工单联动：Done（新增 `POST /booking/four-account-reconcile/refund-commission-audit/sync-tickets`，按筛选结果批量 upsert 统一工单并返回 `attempted/success/failed/failedOrderIds`；异常调用按 fail-open 处理，不阻断批量同步）。
102. booking 退款回调审计与幂等收口：Done（新增 `POST /booking/order/update-refunded`，解析 `merchantRefundId` 兼容 `{orderId}-refund/{orderId}`；`booking_order` 新增 `pay_refund_id/refund_time` 并在回调成功落库；已退款订单同退款单号幂等成功、不同退款单号返回 `BOOKING_ORDER_REFUND_IDEMPOTENT_CONFLICT`，并持续校验退款单存在/成功状态/金额/商户单号一致性）。
103. 四账退款佣金审计后端运营收口：Done（`/booking/four-account-reconcile/page|get` 新增退款审计筛选与响应字段，`runReconcile` 固化退款审计快照；新增 `GET /booking/four-account-reconcile/refund-audit-summary` 返回总数/差异金额聚合/状态聚合/异常类型聚合/未收口工单数；`refundEvidenceJson` 非法时降级标记 `refundEvidenceJsonParseError`，保留原文不中断接口）。
104. booking 退款回调补偿与对账自动闭环：Done（新增台账表 `hxy_booking_refund_notify_log` 与管理端接口 `refund-notify-log/page + replay`；`/booking/order/update-refunded` 成功/失败均全量落账；新增 `BookingRefundReconcileRepairJob` 按 `pay_refund.SUCCESS` 扫描并修复“支付已退但 booking 未退”，单条失败 fail-open 不阻断批次）。
105. 多窗口工程执行治理原则基线化：Done（统一执行口径“最佳实践优先、客观冷静、第一性原理、真实透明”；落地到基线事实、ADR 与协作机制文档，后续以门禁与可复现证据作为集成验收前提）。
106. booking 退款回调补偿运营化 V2：Done（`refund-notify-log/replay` 升级为批量 `id+ids` + `dryRun`，返回 `success/skip/fail + details` 结构化回执；台账新增 `lastReplay*` 审计字段；新增 `BookingRefundNotifyReplayJob` 自动扫描失败记录重放并输出 `runId` 批次摘要；重放成功后触发四账刷新且下游异常 fail-open）。
107. booking 退款回调补偿运营化 V3：Done（新增批次运行台账 `hxy_booking_refund_replay_run_log` 与管理端接口 `replay-due + replay-run-log/page|get`；手工触发与 Job 共用 due-fail 扫描与 runId 审计口径；批次状态按 `success/partial_fail/fail` 汇总判定，单条失败与四账刷新异常均 fail-open 且写入 run-log `errorMsg`）。
108. booking 退款补偿看板与工单联动 V2：Done（新增明细台账 `hxy_booking_refund_replay_run_detail`；新增 `replay-run-log/summary` 输出 `warningCount + topFailCodes + topWarningTags`；新增 `replay-run-log/sync-tickets` 按 runId 批量 upsert 工单并返回 `attempted/success/failed/failedIds`；`replay-run-log/page` 新增 `hasWarning/minFailCount/triggerSource` 筛选并保持兼容）。
109. booking 退款补偿运营闭环 V4：Done（新增工单同步审计台账 `hxy_booking_refund_replay_ticket_sync_log`；新增 `replay-run-log/detail/page|get`；`sync-tickets` 支持 `dryRun/forceResync` 与 `attempted/success/skip/fail + details`，并对“已成功且未强制重放”执行 `SKIP` 幂等；`summary` 新增 `ticketSyncSuccessCount/ticketSyncSkipCount/ticketSyncFailCount` 聚合，跨域异常维持 fail-open）。
110. 财务三项 Partial 全量收口：Done（退款回调非 `PAID/REFUNDED` 改显式报错并可检索；退款主链路中的提成冲正改 fail-open；提成计提新增 `order_item_id/service_order_id/source_biz_no` 追溯字段与 `FULFILLMENT_COMPLETE` 幂等键；trade-api 新增 `listTraceByPayOrderId` 供 booking 回填追溯；四账汇总新增 `commissionAmountSum/commissionDifferenceAbsSum` 聚合字段并保持兼容）。
111. 财务发布就绪门禁收口：Done（新增契约冻结清单 `2026-03-08-finance-contract-freeze-checklist.md`；新增追溯字段回滚脚本 `2026-03-08-hxy-technician-commission-traceability-rollback.sql`；退款回调/replay/提成计提冲正链路补齐结构化日志字段校验，统一输出 `runId/orderId/payRefundId/sourceBizNo/errorCode`，字段缺失仅告警不阻断主流程）。
112. 小程序 P0 契约冻结页级矩阵：Done（`docs/contracts/2026-03-08-miniapp-contract-freeze-v1.md` 完整覆盖支付结果、订单、售后、退款进度、预约、地址、领券、积分商城页面；冻结接口字段、错误码与降级语义并标注能力状态）。
113. 支付结果聚合接口收口：Done（trade 新增 `GET /trade/order/pay-result`，按订单维度返回 `payResultCode + payOrderStatus + degraded`，兼容旧查询接口且不破坏现有语义）。
114. 退款进度聚合接口收口：Done（trade 新增 `GET /trade/after-sale/refund-progress`，支持 `afterSaleId/orderId` 维度，统一输出 `progressCode` 与渠道错误信息；pay 退款单不可用时按售后状态降级返回）。
115. 四账联动/工单同步结构化日志增强：Done（四账 WARN 建单失败、PASS 收口失败、退款-提成工单同步失败、replay 触发四账刷新失败场景统一输出 `runId/orderId/payRefundId/sourceBizNo/errorCode`，并保留 fail-open）。
116. 小程序文档缺口 P0 全量收口：Done（新增 IA 路由总表、统一状态机、错误码恢复矩阵、降级重试手册、发布回滚 SOP，落地路径覆盖 `docs/products/miniapp`、`docs/contracts`、`docs/plans`）。
117. 小程序文档缺口 P1 全量收口：Done（新增活动真值绑定、类目同源刷新、工单审计联动、UI 文案术语规范，确保 shadcn/closure 页面与后端语义一致）。
118. 小程序文档缺口 P2 全量收口：Done（新增埋点漏斗、推荐可解释性、动效性能与无障碍规范，形成发布后优化与可观测基线）。
119. 小程序文档闭环索引与长记忆同步：Done（新增索引 `docs/products/miniapp/2026-03-08-miniapp-doc-gap-closure-index-v1.md`，并同步事实基线与 ADR，避免窗口切换导致知识丢失）。

## 8. 本周优先级（建议）

1. P0：服务/实物分流强约束 + 幂等库存流水。
2. P0：订单模板快照落单。
3. P1：技师提成计提与冲正。
4. P1：结算审批流 + 客服工单SLA最小闭环。
5. P1：门店生命周期守卫 + 管理端联调收口。
120. 小程序 P0 契约回归验收（窗口 D）：Done（新增 `AppTradeOrderControllerTest` 与 `AppAfterSaleControllerTest` 独立回归类；补齐 `pay-result/refund-progress` 正常/查无/降级场景；错误码锚点 `1030004012/1030004016/1011000125` 与 fail-open 可检索场景已纳入自动化并通过门禁命令）。
121. 全域数据价值化蓝图（用户/运营/经营/门店）发布：Done（新增 `hxy/04_data/HXY-全域数据价值化与门店数据治理蓝图-v1-2026-03-08.md`，冻结统一事件协议、统一主键体系、门店最小必采清单、指标口径、DQ门禁与标签策略）。
122. 门店数据“采集即治理”原则入执行基线：Done（明确 Day-0 必采主题：交易、履约、库存、营销、质量、财务；所有主题强制绑定 `tenantId/storeId/orderId/sourceBizNo` 追溯键）。
123. 数据价值闭环里程碑（90天）固化：Done（`0-30天` 完成协议与ODS/DWD、`31-60天` 完成DWS/ADS与工单闭环、`61-90天` 完成策略实验与ROI复盘）。
124. 数据门禁扩展建议收口：Done（在现有 miniapp 合同门禁基础上新增建议项：`event contract gate`、`DQ gate`、`label policy gate`，用于把数据治理从文档态推进到发布阻断态）。
125. uniapp 仓库治理收口：Done（`yudao-mall-uniapp` 已从独立 Git 仓库并入主仓管理，父仓直接追踪目录内容；原 `.git` 已本地备份保留，后续 uniapp 变更统一随 `hxy-java` 主仓提交）。
126. 预约服务评价域稳定性补丁：Done（前端评价 API 已在域内归一化 `false/reject` 失败结果，避免页面直接解构崩溃；后端并发重复评价已把唯一键冲突收敛为 `BOOKING_REVIEW_ALREADY_EXISTS(1030008001)`；后台评价运营页已补菜单 SQL 与 `booking:review:query/update` 权限授权，形成代码/SQL/回归测试三件套闭环）。
127. 预约服务评价字段真值补齐：Done（提交页已接入 `s-uploader` 并真实提交 `picUrls[]`；后端创建评价时按 `payOrderId -> TradeServiceOrderApi.listTraceByPayOrderId` best-effort 回填 `serviceOrderId`，优先同 `spuId/skuId`，trace 失败则 fail-open 保持 `null`；相关前端 smoke 与 booking review service/controller/race-condition 测试已通过）。
128. 预约服务评价历史详情页补齐：Done（小程序新增 `/pages/booking/review-detail`，由 `/pages/booking/review-list` 真实跳转并消费 `GET /booking/review/get`；会员端只展示评分、标签、正文、图片、商家回复与提交时间等标准字段，不暴露 `serviceOrderId/riskLevel/followStatus/auditStatus` 等后台恢复字段；本次仅补 query-side history，不改变 `Can Release=No` 结论）。
129. 预约服务评价负评店长待办最小闭环：Done（新增 SQL `2026-03-18-hxy-booking-review-negative-manager-todo.sql` 扩展 `booking_review` 的负评触发、店长联系人快照、待办状态与三段 SLA 字段；后台评价运营页新增待办筛选、详情卡片、`claim/first-action/close` 动作与看板计数；店长真值第一版只认 `ProductStoreDO.contactName/contactMobile`，不承诺账号级绑定、自动通知、奖励或补偿，结论仍维持 `Doc Closed / Can Develop / Cannot Release`）。
130. 预约服务评价店长双通道通知闭环：Done（SQL `2026-03-21-hxy-booking-review-manager-notify-routing.sql` 已补 `manager_wecom_user_id/receiver_account`；`storeId -> managerAdminUserId + managerWecomUserId` 路由真值、`IN_APP/WECOM` 双通道 outbox、共享企微 sender、`MANAGER_CLAIM_TIMEOUT/MANAGER_FIRST_ACTION_TIMEOUT/MANAGER_CLOSE_TIMEOUT` SLA reminder job、后台 notify/routing/详情观测与 50 项后端 + 22 项 Node 回归已闭环；发布口径继续保持 `Doc Closed / Can Develop / Cannot Release`）。
131. Booking 写链协议收口（第一轮）：Partial（`order-list` 分页协议、`payOrderId`/订单绑定字段、`order-confirm -> slot detail`、`duration` 已真实闭环；但 create 的 `spuId/skuId` 商品来源、addon 的升级/加项目来源、发布级样本与巡检证据仍未闭环，结论继续保持 `Can Develop / Cannot Release`）。
132. Member 缺页能力工程闭环：Done（`/pages/user/level`、`/pages/profile/assets`、`/pages/user/tag` 已形成真实页面 + 路由 + 入口 + API + app 读取链路；`GET /member/tag/my` 与 `GET /member/asset-ledger/page` 已补真实 controller，其中资产总账聚合固定放在 `yudao-server` 以避免模块循环依赖。当前状态已从“缺页补实现”升级为“已完成开发 / 待 release evidence”，结论保持 `Can Develop / Cannot Release`）。
133. BO-004 admin-only 真值闭环：Done（新增独立后台页面 `mall/booking/commission/index.vue`、独立前端 API `api/mall/booking/commission.ts`、菜单 SQL `2026-03-24-hxy-booking-commission-admin-menu.sql`、专项 truth test `technician-commission-admin-truth.test.mjs` 与 `TechnicianCommissionControllerTest`；当前状态已从“仅接口闭环 + 页面真值待核”升级为“admin-only 页面/API 真值已闭环 / Can Develop / Cannot Release”，剩余 blocker 收敛为真实页面请求样本、菜单执行样本、发布证据与写后回读验收样本）。
134. Reserved runtime 二阶段收口：Done（`Referral` 已完成真实页面 `/pages/referral/index` + façade controller `/promotion/referral/*`；`Technician Feed` 已完成真实页面 `/pages/technician/feed`、技师详情真实入口、booking app controller `/booking/technician/feed/page|like|comment/create`、SQL `2026-03-24-hxy-technician-feed-runtime.sql` 与专项前后端测试；`Gift Card` 已完成真实页面 `/pages/gift-card/list|order-detail|redeem|refund`、资产页真实入口 `/pages/profile/assets`、app controller `/promotion/gift-card/*`、SQL `2026-03-24-hxy-gift-card-runtime.sql` 与专项前后端测试。Reserved 全域当前统一结论更新为 `Can Develop / Cannot Release`，剩余 blocker 收敛为 release evidence / gray gate / rollback / sign-off）。
