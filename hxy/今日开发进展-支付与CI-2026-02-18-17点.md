# 今日开发进展（支付与CI）

- 时间：2026-02-18 17:00+ 
- 范围：支付值守回归链路 + 主CI门槛串联

## 已完成

1. `crmeb-ci-gate` 新增前置 job：`payment-ops-smoke`
- 作用：先跑支付离线 smoke，再跑 Docker gate，避免“支付脚本回归问题”晚发现。
- 文件：`crmeb_java/.github/workflows/crmeb-ci-gate.yml`

2. `payment-ops-smoke` 覆盖项已接入主门槛：
- `payment_summary_contract_smoke.sh`
- `payment_ops_status_smoke.sh`
- `payment_decision_chain_smoke.sh`
- `payment_cutover_gate_smoke.sh`
- `payment_ops_cron_smoke.sh`
- `payment_mock_replay.sh`

3. `gate` job 已增加依赖：
- `needs: [payment-ops-smoke]`

4. CI 基线文档已更新：
- 将 `payment-ops offline smoke` 提升为发布链路第一步。
- 文件：`crmeb_java/crmeb/deploy/docker/CICD_BASELINE.md`

5. 新增 smoke 产物统一索引：
- 脚本：`crmeb_java/crmeb/shell/payment_smoke_artifact_index.sh`
- 产物：`crmeb/runtime/payment_ops_smoke_artifact_index.md`
- 作用：在 CI artifact 中快速查看各 smoke 子任务最新 `summary/report/status`，无需逐目录查找。

## 当前结果

- 本地离线 smoke 全通过（上一轮验证）：
  - contract/ops_status/decision_chain/cutover_gate/ops_cron/mock_replay 均 PASS/GREEN。
- 真实环境在线复跑（上一轮验证）：
  - `payment_ops_status --refresh` => `overall=GREEN`
  - `payment_cutover_gate --order-no wxNo89809177138520395483906 ...` => `overall=GREEN, gate_decision=GO`

## 下一步（可继续自动推进）

1. 将 `crmeb-payment-ops-smoke.yml` 调整为 `workflow_call`，避免与 `crmeb-ci-gate` 重复定义步骤。
2. 在 `crmeb-ci-gate` 增加 `concurrency`（同分支取消旧任务），降低资源占用。
3. 为 `payment-cutover-gate` 增加 artifact 上传（summary/report/log），便于 PR 直接审计。
