# 支付系统-D1-D7执行记录

- 更新日期: 2026-02-15
- 执行人: Codex

## 本轮已完成
1. D2 回调安全补强（代码已落地）
   - 新增支付回调签名 key 解析器:
     - `crmeb_java/crmeb/crmeb-service/src/main/java/com/zbkj/service/service/impl/payment/WeChatPaySignKeyResolver.java`
   - 新增微信签名校验工具方法:
     - `crmeb_java/crmeb/crmeb-common/src/main/java/com/zbkj/common/utils/WxPayUtil.java`
   - 支付回调接入“验签 + 二次查单 + 一致性校验”:
     - `crmeb_java/crmeb/crmeb-service/src/main/java/com/zbkj/service/service/impl/CallbackServiceImpl.java`
   - 新增单测:
     - `crmeb_java/crmeb/crmeb-service/src/test/java/com/zbkj/service/service/impl/payment/WeChatPaySignKeyResolverTest.java`
2. D3 对账底座模板
   - 新增日对账 SQL 模板:
     - `crmeb_java/crmeb/sql/payment_reconcile_daily_template.sql`
3. D1-D7主清单状态更新
   - `hxy/支付系统-7天执行清单.md`
4. D5-D7执行文档
   - `hxy/支付系统-D5退款闭环改造清单.md`
   - `hxy/支付系统-D6监控告警清单.md`
   - `hxy/支付系统-D7上线切换Runbook.md`
5. D5 退款闭环代码收口（已落地）
   - 新增退款状态机:
     - `crmeb_java/crmeb/crmeb-service/src/main/java/com/zbkj/service/service/impl/payment/OrderRefundStateMachine.java`
   - 退款任务入口按状态机过滤:
     - `crmeb_java/crmeb/crmeb-service/src/main/java/com/zbkj/service/service/impl/OrderTaskServiceImpl.java`
   - 退款后置处理增加幂等短路（已成功不重复处理）:
     - `crmeb_java/crmeb/crmeb-service/src/main/java/com/zbkj/service/service/impl/StoreOrderTaskServiceImpl.java`
   - 新增状态机单测:
     - `crmeb_java/crmeb/crmeb-service/src/test/java/com/zbkj/service/service/impl/payment/OrderRefundStateMachineTest.java`
   - 新增“退款中超时补偿”:
     - `crmeb_java/crmeb/crmeb-service/src/main/java/com/zbkj/service/service/WechatNewService.java`
     - `crmeb_java/crmeb/crmeb-service/src/main/java/com/zbkj/service/service/impl/WechatNewServiceImpl.java`
     - `crmeb_java/crmeb/crmeb-service/src/main/java/com/zbkj/service/service/impl/OrderTaskServiceImpl.java`
     - `crmeb_java/crmeb/crmeb-common/src/main/java/com/zbkj/common/constants/PayConstants.java`
   - 补偿逻辑：
     - 扫描 `refund_status=3` 且超时订单
     - 调用微信 `refundquery` 二次确认
     - `SUCCESS` 自动收敛为已退款并入后置队列
     - `REFUNDCLOSE/CHANGE` 回退到申请中并打日志待人工
   - 编译验证:
     - `mvn -q -pl crmeb-service -am test-compile` 通过
6. D6 监控告警首版落地（夜间值守可用）
   - 新增快速巡检脚本:
     - `crmeb_java/crmeb/shell/payment_monitor_quickcheck.sh`
   - 新增巡检告警包装器:
     - `crmeb_java/crmeb/shell/payment_monitor_alert.sh`
   - 新增统一告警推送脚本:
     - `crmeb_java/crmeb/shell/payment_alert_notify.sh`
   - 新增定时任务安装器:
     - `crmeb_java/crmeb/shell/payment_ops_cron.sh`
   - 已完成本地验证:
     - `./shell/payment_monitor_quickcheck.sh --window 120 --tail 1000`
     - 结果: 当前未命中异常阈值
7. D3 对账脚本落地（可直接跑 T+1）
   - 新增日对账执行脚本:
     - `crmeb_java/crmeb/shell/payment_reconcile_daily.sh`
   - 已完成 dry-run 验证:
     - `./shell/payment_reconcile_daily.sh --dry-run --date 2026-02-15`
8. D7 全链路演练脚本落地
   - 新增演练脚本:
     - `crmeb_java/crmeb/shell/payment_fullchain_drill.sh`
   - 已完成 dry-run 验证:
     - `./shell/payment_fullchain_drill.sh --dry-run`
9. D3/D6/D7 定时任务已接入当前环境 crontab
   - 执行命令:
     - `./shell/payment_ops_cron.sh --apply`
   - 已生效任务:
     - 每5分钟监控巡检
     - 每日 01:15 日对账
     - 每日 01:30 全链路演练
   - 当前已优化:
     - 飞书 webhook 已恢复配置
     - 支持 `MYSQL_DEFAULTS_FILE`，已移除 crontab 明文 `DB_PASS`
10. D5 第5项“对账联动”已落地
   - 对账脚本新增“退款成功自动消差”能力（A3场景）
   - 产物分层：
     - `main_diff_raw.tsv`（原始差异）
     - `main_diff_cleared_refund.tsv`（退款自动消差）
     - `main_diff.tsv`（最终待处理差异）
   - 告警和退出码仅按 `main_diff.tsv + orphan_wx.tsv` 判定
   - 代码文件：
     - `crmeb_java/crmeb/shell/payment_reconcile_daily.sh`
     - `crmeb_java/crmeb/sql/payment_reconcile_daily_template.sql`
   - 验证：
     - `DB_USER=crmeb DB_PASS=crmeb123 ./shell/payment_reconcile_daily.sh --date 2026-02-15`
     - 输出 `main_raw_diff_count=0, main_cleared_by_refund_count=0, main_diff_count=0, orphan_wx_count=0`
11. D6 监控告警增强（对账联动指标）
   - 对账告警内容已新增：
     - `raw_diff`
     - `auto_cleared_refund`
     - `unresolved_diff`
     - `summary/unresolved_file/cleared_file`
12. D7 上线前预检脚本（新增）
   - `crmeb_java/crmeb/shell/payment_preflight_check.sh`
   - 覆盖：支付配置、回调域名、证书、cron、凭据安全
13. D4 门店映射治理增强（新增）
   - 导入脚本新增运行产物：
     - `summary.txt / plan.tsv / conflicts.tsv / normalized.tsv`
   - 新增回滚脚本：
     - `crmeb_java/crmeb/shell/payment_store_mapping_rollback.sh`
14. 服务商联调包（新增）
   - 一键填参脚本：
     - `crmeb_java/crmeb/shell/payment_service_provider_fill.sh`
   - 联调清单文档：
     - `hxy/支付系统-服务商联调包.md`
15. 日常一键巡检脚本（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_ops_daily.sh`
   - 串行执行：
     - preflight -> monitor -> reconcile
   - 运行产物：
     - `runtime/payment_ops_daily/run-<timestamp>/summary.txt`
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_ops_daily.sh --date 2026-02-15 --no-alert`
     - 结果 `severity=OK`
16. D6 防重入 + 告警降噪 + 日报（新增）
   - cron 防重入：
     - `crmeb_java/crmeb/shell/payment_ops_cron.sh`
     - 监控/对账/演练三条任务已改为 `flock -n` 互斥执行
   - 告警降噪：
     - `crmeb_java/crmeb/shell/payment_alert_notify.sh`
     - 新增同内容抑制窗口（默认 600 秒，可配置 `ALERT_SUPPRESS_SECONDS`）
   - 日报生成：
     - `crmeb_java/crmeb/shell/payment_daily_report.sh`
     - 生成 `runtime/payment_daily_report/<date>/payment_daily_report_<date>.md`
   - 本地验证：
     - `./shell/payment_ops_cron.sh --mysql-defaults-file /root/.my.cnf`（dry-run 已看到 `flock`）
     - `./shell/payment_daily_report.sh --date 2026-02-15`（已生成）
     - `payment_alert_notify.sh` 连续两次相同内容，第二次被抑制
17. D8 + D9 + D12 执行落地（新增）
   - D8 对账差异工单化：
     - `crmeb_java/crmeb/shell/payment_reconcile_ticketize.sh`
     - 产物：`tickets.tsv`、`tickets.md`
   - D9 运行产物留存治理：
     - `crmeb_java/crmeb/shell/payment_runtime_retention.sh`
     - 支持 `--keep-days`、`--apply`，默认 dry-run
   - D12 巡检+日报自动串联：
     - `crmeb_java/crmeb/shell/payment_daily_report_notify.sh`
     - `payment_ops_cron.sh` 新增 `--report-notify` 与日报 cron 任务
   - 本地验证：
     - `payment_reconcile_ticketize.sh --date 2026-02-15`（无差异场景返回 2 并生成空工单）
     - `payment_runtime_retention.sh --keep-days 7`（dry-run 报告已生成）
     - `payment_daily_report_notify.sh --date 2026-02-15`（未配 webhook 场景 skip）
     - `payment_ops_cron.sh --apply --report-notify 1` 已生效（含日报任务）
18. D11 门店映射严格校验增强（新增）
   - 增强脚本：
     - `crmeb_java/crmeb/shell/payment_store_mapping_import.sh`
   - 新增能力：
     - `--strict-submchid-format`（10位数字强校验）
     - `--conflict-strategy block|overwrite`
     - `--apply --confirm` 二次确认
   - 本地验证：
     - 严格格式非法输入被拦截（rc=1）
     - `block` 策略冲突返回 rc=2
     - `overwrite` 策略可通过 dry-run（rc=0）
     - `--apply` 未加 `--confirm` 被拦截（rc=3）
19. D13 幂等回归检查包（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_idempotency_regression.sh`
   - 检查覆盖：
     - 重复支付成功日志
     - 重复退款成功日志
     - 已支付无流水 / 渠道状态异常
     - 已退款无成功日志 / 退款处理中超时
     - 重复 out_trade_no / 非法 refund_status
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_idempotency_regression.sh --window-hours 72 --no-alert`（rc=0）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_idempotency_regression.sh --order-no <已支付订单号> --window-hours 720 --no-alert`（rc=0）
20. D14 切换预演包（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_cutover_rehearsal.sh`
   - 能力：
     - 无订单号阶段完成上线切换预演并输出统一结论
     - 结论字段：`ready_for_order_drill=0|1`
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_cutover_rehearsal.sh --date 2026-02-15 --window-hours 72 --no-alert`
     - 输出预演报告与 summary（当前 strict preflight 为 WARN，结论为未通过）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_cutover_rehearsal.sh --non-strict-preflight --no-alert`（rc=0，ready_for_order_drill=1）
21. D14 配置快照与回滚能力（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_config_snapshot.sh`
     - `crmeb_java/crmeb/shell/payment_config_restore.sh`
   - 能力：
     - 切换前导出支付关键配置快照与 `restore.sql`
     - 支持 `--latest` 预览/执行回滚
   - 兼容改造：
     - `payment_service_provider_fill.sh --apply` 默认自动快照（可 `--no-snapshot` 关闭）
   - 本地验证：
     - `payment_config_snapshot.sh --tag pre-cutover-test` 成功生成 `snapshot.tsv/restore.sql/summary.txt`
     - `payment_config_restore.sh --latest` 可正确预览回滚SQL
22. D14 切换编排脚本（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_cutover_apply.sh`
   - 能力：
     - dry-run / apply 双模式
     - apply 默认失败自动回滚（配置 + 门店映射）
     - 输出统一编排报告（ready/rolled_back）
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_cutover_apply.sh --appid ... --sp-mchid ... --sp-key ... --sub-mchid ... --non-strict-preflight --no-alert`（rc=0）
23. D12 + D9 运维闭环增强（新增）
   - `payment_ops_cron.sh` 新增 warroom 任务参数：
     - `--warroom-notify 0|1`
     - `--warroom-cron EXPR`
     - `--warroom-window-hours N`
   - `payment_runtime_retention.sh` 纳管目录扩展：
     - `payment_idempotency_regression`
     - `payment_cutover_rehearsal`
     - `payment_cutover_apply`
     - `payment_config_snapshot`
     - `payment_warroom`
   - 本地验证：
     - `./shell/payment_ops_cron.sh --mysql-defaults-file /root/.my.cnf --report-notify 1 --warroom-notify 1 --warroom-window-hours 72`（dry-run rc=0）
     - `./shell/payment_runtime_retention.sh --keep-days 7`（dry-run rc=0，新增目录扫描通过）
     - `ALERT_WEBHOOK_TYPE='feishu' ALERT_WEBHOOK_URL='***' ./shell/payment_ops_cron.sh --apply --mysql-defaults-file /root/.my.cnf --report-notify 1 --warroom-notify 1`（rc=0）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_warroom_dashboard.sh --date 2026-02-15 --window-hours 72 --no-alert`（rc=0，overall=GREEN）
24. D10 上线判定一页化（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_go_nogo_decision.sh`
   - 能力：
     - 无订单号阶段输出 `GO_FOR_ORDER_DRILL/NO_GO`
     - 有订单号阶段自动执行 `payment_fullchain_drill.sh --order-no ...` 并输出 `GO_LAUNCH/NO_GO`
     - 统一产物：`runtime/payment_go_nogo/run-*/summary.txt + decision.md`
   - 配套治理：
     - `payment_runtime_retention.sh` 已纳管 `payment_go_nogo`
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_go_nogo_decision.sh --no-alert`（rc=0，decision=GO_FOR_ORDER_DRILL）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_go_nogo_decision.sh --require-apply-ready 1 --no-alert`（rc=2，decision=NO_GO）
25. D6/D12 值守闭环补强（新增）
   - `payment_ops_cron.sh` 新增 go/no-go 定时参数：
     - `--go-nogo-notify 0|1`
     - `--go-nogo-cron EXPR`
     - `--go-nogo-require-apply-ready 0|1`
   - `payment_warroom_dashboard.sh` 已并入 latest go/no-go 判定状态
   - 本地验证：
     - `./shell/payment_ops_cron.sh --mysql-defaults-file /root/.my.cnf --report-notify 1 --warroom-notify 1 --go-nogo-notify 1 --go-nogo-require-apply-ready 0`（dry-run rc=0）
     - `ALERT_WEBHOOK_TYPE='feishu' ALERT_WEBHOOK_URL='***' ./shell/payment_ops_cron.sh --apply --mysql-defaults-file /root/.my.cnf --report-notify 1 --warroom-notify 1 --go-nogo-notify 1 --go-nogo-require-apply-ready 0`（rc=0）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_warroom_dashboard.sh --date 2026-02-15 --window-hours 72 --no-alert`（rc=0）
     - `crontab` 托管任务数已为 6（含 go-nogo）
26. D12 值守自动化再增强（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_ops_morning_bundle.sh`
   - 能力：
     - 串行执行 `ops_daily -> go_nogo -> warroom`
     - 输出统一 `summary.txt + report.md`
     - 只推送一条汇总告警（避免多脚本重复告警）
   - `payment_ops_cron.sh` 新增 morning-bundle 定时参数：
     - `--morning-bundle-notify 0|1`
     - `--morning-bundle-cron EXPR`
     - `--morning-bundle-require-apply-ready 0|1`
     - `--morning-bundle-window-hours N`
     - `--morning-bundle-window N`
     - `--morning-bundle-tail N`
     - `--morning-bundle-strict-preflight 0|1`
   - 配套治理：
     - `payment_runtime_retention.sh` 已纳管 `payment_ops_morning_bundle`
     - `payment_preflight_check.sh` 已纳管 `payment_ops_morning_bundle.sh` 存在性检查
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_ops_morning_bundle.sh --date 2026-02-15 --no-alert`（rc=0，severity=OK）
     - `./shell/payment_ops_cron.sh --mysql-defaults-file /root/.my.cnf --report-notify 1 --warroom-notify 1 --go-nogo-notify 1 --morning-bundle-notify 1 --status-notify 1 --contract-notify 1 --retention-notify 1`（dry-run rc=0）
     - `ALERT_WEBHOOK_TYPE='feishu' ALERT_WEBHOOK_URL='***' ./shell/payment_ops_cron.sh --apply --mysql-defaults-file /root/.my.cnf --report-notify 1 --warroom-notify 1 --go-nogo-notify 1 --morning-bundle-notify 1 --status-notify 1 --contract-notify 1 --retention-notify 1`（rc=0）
     - `crontab` 托管任务数已为 10（含 morning-bundle + status + contract + retention）
27. D12 判定链路稳定性修复（新增）
   - 修复点：
     - `payment_go_nogo_decision.sh` 与 `payment_warroom_dashboard.sh` 的 latest summary 选择改为按 mtime，避免同秒多次运行时取错旧文件。
     - `payment_go_nogo_decision.sh` 去除对 warroom 的阻断依赖（保留参考指标），避免与 warroom 形成循环红灯。
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_go_nogo_decision.sh --require-apply-ready 0 --no-alert`（rc=0，GO_FOR_ORDER_DRILL）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_warroom_dashboard.sh --date 2026-02-15 --window-hours 72 --no-alert`（rc=0，overall=GREEN）
28. D15 + D16 值守可观测性补强（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_ops_status.sh`
     - `crmeb_java/crmeb/shell/payment_summary_contract_check.sh`
   - 配套修正：
     - `payment_preflight_check.sh` 已纳管上述两个脚本存在性检查
     - `payment_runtime_retention.sh` 已纳管 `payment_ops_status/payment_contract_check`
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_ops_status.sh --date 2026-02-15 --no-alert`（rc=0，overall=GREEN）
     - `./shell/payment_summary_contract_check.sh --date 2026-02-15 --no-alert`（rc=0，warn=1，fail=0）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_preflight_check.sh`（PASS=15，WARN=4，FAIL=0）
     - `./shell/payment_runtime_retention.sh --keep-days 7`（dry-run rc=0，新增目录扫描通过）
29. D12 定时任务编排扩展（新增）
   - `payment_ops_cron.sh` 新增参数：
     - `--status-notify/--status-cron/--status-refresh/--status-refresh-require-apply-ready`
     - `--contract-notify/--contract-cron/--contract-require-all`
     - `--sla-notify/--sla-cron/--sla-lookback-days/--sla-days`
   - 可选任务新增：
     - `payment_ops_status.sh`（值守总览）
     - `payment_summary_contract_check.sh`（summary 协议回归）
     - `payment_reconcile_sla_guard.sh`（对账SLA守卫）
   - 本地验证：
     - `./shell/payment_ops_cron.sh --mysql-defaults-file /root/.my.cnf --report-notify 1 --warroom-notify 1 --go-nogo-notify 1 --morning-bundle-notify 1 --status-notify 1 --contract-notify 1 --sla-notify 1 --sla-lookback-days 14 --sla-days 1 --retention-notify 1`（dry-run rc=0）
     - `ALERT_WEBHOOK_TYPE='feishu' ALERT_WEBHOOK_URL='***' ./shell/payment_ops_cron.sh --apply --mysql-defaults-file /root/.my.cnf --report-notify 1 --warroom-notify 1 --go-nogo-notify 1 --morning-bundle-notify 1 --status-notify 1 --contract-notify 1 --sla-notify 1 --sla-lookback-days 14 --sla-days 1 --retention-notify 1`（rc=0）
     - 托管任务数由 8 增至 11（新增 status + contract + reconcile_sla）
30. D11 门店映射模板导出能力（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_store_mapping_template_export.sh`
   - 能力：
     - 自动读取 `eb_system_store` 生成 `storeId,sub_mchid` 导入模板
     - 同步输出门店参考清单（含名称/电话/当前映射）
     - 支持 `--blank-sub-mchid` 强制空白模板
   - 配套修正：
     - `payment_preflight_check.sh` 纳管脚本存在性检查
     - `payment_runtime_retention.sh` 纳管 `payment_store_mapping` 历史目录
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_store_mapping_template_export.sh`（rc=0，导出2家门店）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_preflight_check.sh`（PASS=16，WARN=4，FAIL=0）
     - `./shell/payment_runtime_retention.sh --keep-days 7`（dry-run rc=0，新增目录扫描通过）
31. D16 协议回归检查修正（新增）
   - 修复点：
     - `payment_summary_contract_check.sh` 中 `store_mapping_import` 字段合同改为与实际 summary 对齐
     - 新增 `store_mapping_template_export` summary 合同检查
   - 本地验证：
     - `./shell/payment_summary_contract_check.sh --date 2026-02-15 --no-alert`（rc=0，warn=0，fail=0）
     - `./shell/payment_summary_contract_check.sh --date 2026-02-15 --require-all --no-alert`（rc=0）
32. D17 对账SLA守卫（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_reconcile_sla_guard.sh`
   - 能力：
     - 最近N天差异按 SLA 自动分类为 `OK/PENDING/BREACH`
     - 输出 `summary/report/detail` 并可触发告警
   - 链路接入：
     - `payment_warroom_dashboard.sh` 纳入 `latest_sla_summary` 与风险判定
     - `payment_ops_status.sh` 纳入 `reconcile_sla` 判定
     - `payment_daily_report.sh` 新增 SLA 汇总段
     - `payment_summary_contract_check.sh` 纳入 `reconcile_sla` 协议合同检查
   - 本地验证：
   - `./shell/payment_reconcile_sla_guard.sh --lookback-days 14 --sla-days 1 --no-alert`（rc=0，severity=OK）
   - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_warroom_dashboard.sh --date 2026-02-15 --window-hours 72 --no-alert`（rc=0，overall=GREEN）
   - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_ops_status.sh --date 2026-02-15 --refresh --sla-lookback-days 14 --sla-days 1 --no-alert`（rc=0，overall=GREEN）
33. D18 协议检查离线自测 + 早班包SLA并入（新增）
   - 脚本增强：
     - `crmeb_java/crmeb/shell/payment_ops_morning_bundle.sh`
       - 新增参数：`--sla-lookback-days/--sla-days`
       - 新增步骤：`reconcile_sla`（并入早班报告与 summary）
     - `crmeb_java/crmeb/shell/payment_summary_contract_check.sh`
       - 新增参数：`--runtime-root`
       - `morning_bundle` 合同新增 `sla_rc/sla_severity` 字段检查
     - `crmeb_java/crmeb/shell/payment_summary_contract_smoke.sh`（新增）
       - 离线构造 pass/fail 样本，验证合同检查返回码
     - `crmeb_java/crmeb/shell/payment_preflight_check.sh`
       - 纳管 `payment_summary_contract_smoke.sh` 存在性检查
   - 本地验证：
   - `./shell/payment_ops_morning_bundle.sh --date 2026-02-15 --sla-lookback-days 14 --sla-days 1 --no-alert`（rc=0）
   - `./shell/payment_summary_contract_check.sh --date 2026-02-15 --require-all --no-alert`（rc=0）
   - `./shell/payment_summary_contract_smoke.sh --date 2026-02-15`（pass_case=0，fail_case=2）
34. D19 值守新鲜度守卫（新增）
   - 增强脚本：
     - `crmeb_java/crmeb/shell/payment_ops_status.sh`
       - 新增参数：`--runtime-root`
       - 新增参数：`--max-summary-age-minutes/--max-recon-age-days/--max-daily-report-age-days`
       - 新增输出：“数据新鲜度”分段及各文件 `age_minutes`
   - `payment_ops_cron.sh` 透传新增参数：
     - `--status-max-summary-age-minutes`
     - `--status-max-recon-age-days`
     - `--status-max-daily-report-age-days`
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_ops_status.sh --date 2026-02-15 --refresh --max-summary-age-minutes 240 --max-recon-age-days 2 --max-daily-report-age-days 2 --no-alert`（rc=0，overall=GREEN）
     - `ALERT_WEBHOOK_TYPE='feishu' ALERT_WEBHOOK_URL='***' ./shell/payment_ops_cron.sh --apply --mysql-defaults-file /root/.my.cnf --report-notify 1 --warroom-notify 1 --go-nogo-notify 1 --morning-bundle-notify 1 --status-notify 1 --status-refresh 1 --status-max-summary-age-minutes 240 --status-max-recon-age-days 2 --status-max-daily-report-age-days 2 --contract-notify 1 --sla-notify 1 --sla-lookback-days 14 --sla-days 1 --retention-notify 1`（rc=0）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_preflight_check.sh`（PASS=21，WARN=4，FAIL=0）
35. D20 值守总览离线自测（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_ops_status_smoke.sh`
   - 能力：
     - 离线构造 runtime 样本，验证 `ops_status` 在正常/过期场景返回码
     - 预期：`pass_case=0`、`stale_case=2`
   - 配套修正：
     - `payment_preflight_check.sh` 纳管 `payment_ops_status_smoke.sh`
     - `payment_runtime_retention.sh` 纳管 `payment_ops_status_smoke`
   - 本地验证：
     - `./shell/payment_ops_status_smoke.sh --date 2026-02-15`（rc=0，pass_case=0，stale_case=2）
36. D21 对账工单自动分级（新增）
   - 增强脚本：
     - `crmeb_java/crmeb/shell/payment_reconcile_ticketize.sh`
       - 新增参数：`--sla-summary/--sla-detail`
       - 新增参数：`--amount-p1-threshold-cent`
       - 新增参数：`--no-alert`
       - 新增输出：`tickets/summary.txt`
       - 新增能力：按 SLA BREACH / 金额阈值自动升 P1，并推送告警
   - 链路接入：
     - `payment_ops_cron.sh` 新增：
       - `--ticket-notify/--ticket-cron/--ticket-max-rows/--ticket-amount-p1-threshold-cent`
     - `payment_preflight_check.sh` 纳管：
       - `payment_reconcile_ticketize.sh` 脚本检查
       - cron 工单任务存在性检查
     - `payment_summary_contract_check.sh` 纳入：
       - `reconcile_ticketize(<date>)` 协议检查
     - `payment_ops_status.sh` 纳入：
       - `ticketize_summary` 聚合与新鲜度守卫
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_reconcile_ticketize.sh --date 2026-02-15 --amount-p1-threshold-cent 100000 --no-alert`（rc=2，无差异工单）
     - `./shell/payment_summary_contract_check.sh --date 2026-02-15 --require-all --no-alert`（rc=0）
     - `ALERT_WEBHOOK_TYPE='feishu' ALERT_WEBHOOK_URL='***' ./shell/payment_ops_cron.sh --apply --mysql-defaults-file /root/.my.cnf --report-notify 1 --ticket-notify 1 --ticket-max-rows 200 --ticket-amount-p1-threshold-cent 100000 --warroom-notify 1 --go-nogo-notify 1 --morning-bundle-notify 1 --status-notify 1 --status-refresh 1 --status-max-summary-age-minutes 240 --status-max-recon-age-days 2 --status-max-daily-report-age-days 2 --contract-notify 1 --sla-notify 1 --sla-lookback-days 14 --sla-days 1 --retention-notify 1`（rc=0）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_preflight_check.sh`（PASS=23，WARN=4，FAIL=0）
37. D22 看板/日报工单联动（新增）
   - 增强脚本：
     - `crmeb_java/crmeb/shell/payment_warroom_dashboard.sh`
       - 新增 `ticketize_summary` 聚合
       - 新增字段：`ticket_total/ticket_p1/ticket_p2/ticket_escalated/ticket_sla_status`
       - 风险规则新增：`ticket_p1>0` 计入风险
     - `crmeb_java/crmeb/shell/payment_daily_report.sh`
       - 新增“对账工单”分段
       - 群摘要新增工单行
   - 配套修正：
     - `payment_summary_contract_check.sh` 的 `warroom` 合同新增 `ticketize_summary/ticket_p1`
     - `payment_summary_contract_smoke.sh` 夹具同步 warroom 新字段
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_daily_report.sh --date 2026-02-15`（rc=0）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_warroom_dashboard.sh --date 2026-02-15 --window-hours 72 --no-alert`（rc=0，overall=GREEN）
     - `./shell/payment_summary_contract_check.sh --date 2026-02-15 --require-all --no-alert`（rc=0）
38. D23 工单升级看板 + go/no-go 硬门槛（新增）
   - 增强脚本：
     - `crmeb_java/crmeb/shell/payment_reconcile_ticketize.sh`
       - 新增参数：`--p1-sla-hours/--p2-sla-hours`
       - 新增输出：`tickets/escalation.tsv`、`tickets/escalation.md`
       - `tickets/summary.txt` 新增：`escalation_tsv/escalation_md/p1_sla_hours/p2_sla_hours`
     - `crmeb_java/crmeb/shell/payment_go_nogo_decision.sh`
       - 新增参数：`--date/--ticket-summary`
       - 新增门槛：`ticketize summary` 缺失或 `p1_count>0` 直接 NO_GO
       - 新增输出：`ticket_gate_ok/ticket_total/ticket_p1/ticket_p2`
   - 配套修正：
     - `payment_summary_contract_check.sh`
       - `reconcile_ticketize` 合同新增 `escalation_md`
       - `go_nogo` 合同新增 `report_date/ticket_summary/ticket_gate_ok/ticket_p1`
     - `payment_summary_contract_smoke.sh` 夹具同步新字段
     - `payment_ops_morning_bundle.sh` 透传 `--date` 到 `payment_go_nogo_decision.sh`，避免跨日误判 NO_GO
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_reconcile_ticketize.sh --date 2026-02-15 --amount-p1-threshold-cent 100000 --no-alert`（rc=2，生成 escalation 看板）
     - `./shell/payment_go_nogo_decision.sh --date 2026-02-15 --no-alert`（rc=0，decision=GO_FOR_ORDER_DRILL）
     - `./shell/payment_summary_contract_check.sh --date 2026-02-15 --require-all --no-alert`（rc=0）
39. D24 预约核销 + 会员卡扣减闭环（新增）
   - 新增请求对象：
     - `crmeb_java/crmeb/crmeb-common/src/main/java/com/zbkj/common/request/ServiceBookingVerifyRequest.java`
   - 新增 DAO：
     - `crmeb_java/crmeb/crmeb-service/src/main/java/com/zbkj/service/dao/MemberCardDao.java`
     - `crmeb_java/crmeb/crmeb-service/src/main/java/com/zbkj/service/dao/MemberCardUsageDao.java`
   - 增强服务：
     - `ServiceBookingService` 新增 `verify(ServiceBookingVerifyRequest)`
     - `ServiceBookingServiceImpl` 新增：
       - 已支付预约单核销（2 -> 3）状态机
       - 时间槽状态 `booked -> verified`
       - 会员卡扣减（次卡/储值卡）并发锁保护
       - 会员卡核销流水写入 `eb_member_card_usage`
   - 新增后台接口：
     - `POST /api/admin/booking/order/verify`
   - 数据库补丁：
     - `hxy/database/payment_booking_verify_member_card_usage.sql`
   - 本地验证：
     - `mysql --defaults-file=/root/.my.cnf crmeb_java < /root/crmeb-java/hxy/database/payment_booking_verify_member_card_usage.sql`（表创建成功）
     - `mvn -pl crmeb-common,crmeb-service,crmeb-admin -am -DskipTests compile`（BUILD SUCCESS）
40. D25 预约核销回归检查包（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_booking_verify_regression.sh`
   - 检查项：
     - 已核销但 `check_in_time` 为空
     - 已核销但 `locked_expire` 未清理
     - 绑卡订单已核销但无核销流水
     - 核销流水负值
     - 会员卡状态与余额冲突（Warn）
     - 单预约订单多条核销流水（Warn）
   - 配套修正：
     - `payment_preflight_check.sh` 纳管脚本存在性检查
     - `payment_runtime_retention.sh` 纳管 `payment_booking_verify_regression` 目录
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_booking_verify_regression.sh --window-hours 72 --no-alert`（rc=0，severity=PASS）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_preflight_check.sh`（PASS=24，WARN=4，FAIL=0）
     - `mvn -pl crmeb-common,crmeb-service,crmeb-admin -am -DskipTests compile`（BUILD SUCCESS）
41. D12+D25 值守串联增强 + 核销单测补齐（新增）
   - 脚本增强：
     - `crmeb_java/crmeb/shell/payment_ops_morning_bundle.sh`
       - 新增 `05_booking_verify_regression` 步骤，早班一键包自动串联预约核销一致性检查
       - `summary/report` 输出新增 `booking_verify_*` 字段（`rc/severity/critical/warn`）
     - `crmeb_java/crmeb/shell/payment_booking_verify_regression.sh`
       - 新增控制台输出：`summary=...`，便于上层编排稳定解析
   - 单元测试补齐：
     - `crmeb_java/crmeb/crmeb-service/src/test/java/com/zbkj/service/service/impl/ServiceBookingServiceImplTest.java`
       - 覆盖 `checkMemberCard` 可用性分支
       - 覆盖 `listVerifyRecords` 映射
       - 覆盖 `verify` 成功/核销码不匹配/非已支付状态分支
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_booking_verify_regression.sh --window-hours 72 --no-alert`（rc=0，severity=PASS）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_ops_morning_bundle.sh --date 2026-02-15 --no-alert`（rc=0，包含 `booking_verify_rc=0`）
     - `mvn -pl crmeb-common,crmeb-service -am -Dtest=ServiceBookingServiceImplTest -DfailIfNoTests=false test`（BUILD SUCCESS；当前 surefire 基线未实际执行 JUnit5 用例）
42. D24 单测可执行性修复 + D26 自动修复脚本（新增）
   - 测试执行修复：
     - `crmeb_java/crmeb/crmeb-service/pom.xml`
       - 新增 `maven-surefire-plugin:2.22.2`，使 JUnit5 用例可被 surefire 发现和执行
     - `crmeb_java/crmeb/crmeb-service/src/test/java/com/zbkj/service/service/impl/ServiceBookingServiceImplTest.java`
       - 修复 Mockito 严格模式报错（`lenient`）
       - 修复 MyBatis-Plus Lambda 缓存缺失（初始化实体 TableInfo）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_booking_verify_repair.sh`
       - dry-run/apply 两种模式
       - 自动修复：`check_in_time` 缺失、`locked_expire` 残留
       - 不触碰资金相关异常（流水缺失/余额冲突）
   - 配套修正：
     - `payment_preflight_check.sh` 纳管 `payment_booking_verify_repair.sh`
     - `payment_runtime_retention.sh` 纳管 `payment_booking_verify_repair` 目录
   - 本地验证：
     - `mvn -pl crmeb-common,crmeb-service -am -Dtest=ServiceBookingServiceImplTest -DfailIfNoTests=false test`（Tests run: 6, Failures: 0, Errors: 0）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_booking_verify_repair.sh --window-hours 72 --no-alert`（rc=0，severity=PASS）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_preflight_check.sh`（PASS=25，WARN=4，FAIL=0）
43. D27 核销修复值守联动 + 可选门槛（新增）
   - 脚本增强：
     - `crmeb_java/crmeb/shell/payment_ops_morning_bundle.sh`
       - 新增 `06_booking_verify_repair` 步骤
       - 新增参数：`--require-booking-repair-pass 0|1`
       - summary/report 新增 `booking_repair_*` 字段
     - `crmeb_java/crmeb/shell/payment_go_nogo_decision.sh`
       - 新增参数：`--require-booking-repair-pass 0|1`（默认 0）
       - 开启后要求 latest `booking_verify_repair` 为 PASS，否则直接 NO_GO
     - `crmeb_java/crmeb/shell/payment_ops_status.sh`
       - 聚合 latest `booking_verify_repair` 与 `booking_verify_regression` 状态并纳入风险判定
     - `crmeb_java/crmeb/shell/payment_warroom_dashboard.sh`
       - 看板新增 `booking_verify_repair` 行，`PENDING/ALERT` 计入 `risk_count`
     - `crmeb_java/crmeb/shell/payment_daily_report.sh`
       - 日报新增“预约核销修复”分段与群摘要
     - `crmeb_java/crmeb/shell/payment_summary_contract_check.sh`
       - 合同新增 `booking_verify_repair/booking_verify_regression` 及相关新字段检查
     - `crmeb_java/crmeb/shell/payment_ops_cron.sh`
       - 新增参数：`--go-nogo-require-booking-repair-pass`、`--morning-bundle-require-booking-repair-pass`
   - 本地验证：
     - `bash -n` 通过：`payment_ops_morning_bundle.sh/payment_go_nogo_decision.sh/payment_ops_status.sh/payment_warroom_dashboard.sh/payment_daily_report.sh/payment_summary_contract_check.sh/payment_ops_cron.sh`
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_ops_morning_bundle.sh --no-alert`（rc=2，含 `booking_repair_rc=0`）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_go_nogo_decision.sh --no-alert --require-booking-repair-pass 1`（rc=2，decision=NO_GO）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_warroom_dashboard.sh --no-alert`（rc=2，含 booking_repair 风险聚合）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_daily_report.sh`（rc=0）
     - `./shell/payment_summary_contract_check.sh --no-alert`（rc=0，warn=2，fail=0）
     - `./shell/payment_ops_cron.sh --mysql-defaults-file /root/.my.cnf --report-notify 1 --ticket-notify 1 --warroom-notify 1 --go-nogo-notify 1 --go-nogo-require-booking-repair-pass 1 --morning-bundle-notify 1 --morning-bundle-require-booking-repair-pass 1 --status-notify 1 --status-refresh 1 --contract-notify 1 --sla-notify 1 --retention-notify 1 --booking-repair-notify 1`（dry-run rc=0）
44. D28 核销修复自动apply编排 + status硬门槛参数（新增）
   - 脚本增强：
     - `crmeb_java/crmeb/shell/payment_ops_cron.sh`
       - 新增参数：`--booking-repair-apply 0|1`（默认 0=dry-run，1=自动修复）
       - 新增参数：`--status-require-booking-repair-pass 0|1`
       - `status` cron 命令透传：`--require-booking-repair-pass`
       - `booking_repair` cron 命令在 `--booking-repair-apply 1` 时自动附加 `--apply`
     - `crmeb_java/crmeb/shell/payment_ops_status.sh`
       - 新增参数：`--require-booking-repair-pass 0|1`
       - refresh 场景透传到 morning-bundle/go-no-go
       - 开启后 `booking_verify_repair` 非 `PASS` 或 summary 缺失直接 block
   - 本地验证：
     - `bash -n payment_ops_status.sh payment_ops_cron.sh`（语法通过）
     - `./shell/payment_ops_cron.sh --mysql-defaults-file /root/.my.cnf --status-notify 1 --status-refresh 1 --status-require-booking-repair-pass 1 --booking-repair-notify 1 --booking-repair-window-hours 72 --booking-repair-apply 1 --go-nogo-notify 1 --go-nogo-require-booking-repair-pass 1 --morning-bundle-notify 1 --morning-bundle-require-booking-repair-pass 1`（dry-run rc=0，命令块包含新增参数）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_ops_status.sh --no-alert --require-booking-repair-pass 1`（rc=2，summary 含 `require_booking_repair_pass=1`）
45. D29 值守告警降噪策略（新增）
   - 脚本增强：
     - `crmeb_java/crmeb/shell/payment_ops_status.sh`
       - `apply_summary` 新鲜度告警改为“仅在 `refresh-require-apply-ready=1` 时检查”
       - 目的：无服务商正式切换阶段避免长期 `YELLOW` 假告警
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_ops_status.sh --refresh --require-booking-repair-pass 1 --no-alert`（rc=0，overall=GREEN，warn=0，block=0）
46. D30 值守汇总“按日期优先”选择修复（新增）
   - 背景：
     - `ops_status` 原先读取 `warroom/go_nogo` 的全局 latest，可能误拿到其他日期产物，导致误判。
   - 脚本增强：
     - `crmeb_java/crmeb/shell/payment_ops_status.sh`
       - 新增 `latest_summary_by_key/resolve_summary` 逻辑
       - `ops_daily(m.recon_date)/morning_bundle(report_date)/warroom(report_date)/go_nogo(report_date)/cutover_rehearsal(recon_date)` 改为“同日优先，fallback latest”
     - `crmeb_java/crmeb/shell/payment_ops_status_smoke.sh`
       - 夹具补齐 `report_date/recon_date`
       - 夹具补齐 `booking_verify_regression`/`booking_verify_repair` summary，保证 `pass_case` 稳定为 `rc=0`
   - 本地验证：
     - `./shell/payment_ops_status_smoke.sh --date 2026-02-14`（rc=0，pass_case_rc=0，stale_case_rc=2）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_ops_status.sh --refresh --require-booking-repair-pass 1 --no-alert`（rc=0，overall=GREEN；warroom/go_nogo report_date 均为 2026-02-14）
47. D31 判定链路同日优先修复（新增）
   - 背景：
     - `go_nogo/warroom` 原先也读取全局 latest，存在跨日期误读风险。
   - 脚本增强：
     - `crmeb_java/crmeb/shell/payment_go_nogo_decision.sh`
       - `warroom_summary` 改为 `report_date` 同日优先
       - `rehearsal_summary` 改为 `recon_date` 同日优先
     - `crmeb_java/crmeb/shell/payment_warroom_dashboard.sh`
       - `latest_go_nogo_summary` 改为 `report_date` 同日优先
       - `latest_rehearsal_summary` 改为 `recon_date` 同日优先
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_go_nogo_decision.sh --date 2026-02-14 --require-booking-repair-pass 1 --no-alert`（rc=0，decision=GO_FOR_ORDER_DRILL）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_warroom_dashboard.sh --date 2026-02-14 --window-hours 72 --no-alert`（rc=0，overall=GREEN）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_ops_status.sh --refresh --require-booking-repair-pass 1 --no-alert`（rc=0，overall=GREEN，warroom/go_nogo report_date=2026-02-14）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_preflight_check.sh`（PASS=26，WARN=4，FAIL=0）
48. D32 判定链路同日优先离线自测（新增）
   - 背景：
     - D31 修复后缺少独立离线回归，后续脚本调整时存在回退风险。
   - 脚本新增/增强：
     - 新增 `crmeb_java/crmeb/shell/payment_decision_chain_smoke.sh`
       - 离线构造“同日 summary + 更新但跨日 summary”夹具
       - 校验 `go_nogo` 选择 `warroom(report_date=目标日)` 与 `rehearsal(recon_date=目标日)`
       - 校验 `warroom` 选择 `go_nogo(report_date=目标日)` 与 `rehearsal(recon_date=目标日)`
     - `crmeb_java/crmeb/shell/payment_preflight_check.sh`
       - `required_scripts` 新增 `payment_decision_chain_smoke.sh`
     - `crmeb_java/crmeb/shell/payment_runtime_retention.sh`
       - 纳管目录新增 `payment_decision_chain_smoke`
   - 本地验证：
     - `./shell/payment_decision_chain_smoke.sh`（rc=0，severity=PASS）
     - `./shell/payment_summary_contract_smoke.sh`（rc=0，severity=PASS）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_preflight_check.sh`（PASS=27，WARN=4，FAIL=0）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_ops_status.sh --refresh --require-booking-repair-pass 1 --no-alert`（rc=0，overall=GREEN）
49. D33 日期一致性硬告警 + cron纳管判定链路自测（新增）
   - 背景：
     - D31/D32 已做到“同日优先+离线回归”，但线上仍缺“跨日引用硬告警”和“定时自测纳管”。
   - 脚本增强：
     - `crmeb_java/crmeb/shell/payment_go_nogo_decision.sh`
       - 新增输出字段：`warroom_report_date/warroom_date_match/rehearsal_recon_date/rehearsal_date_match`
       - 若命中跨日 summary（date_match=0）直接加入 blocker，避免误判 GO
     - `crmeb_java/crmeb/shell/payment_warroom_dashboard.sh`
       - 新增输出字段：`go_nogo_report_date/go_nogo_date_match/rehearsal_recon_date/rehearsal_date_match`
       - 任一 date_match=0 计入 `risk_count`，驱动 `overall=RED`
     - `crmeb_java/crmeb/shell/payment_ops_status.sh`
       - 聚合并校验 go_nogo/warroom 的 date_match 字段
       - 对跨日引用直接 block，缺字段降级为 warn（兼容旧产物）
     - `crmeb_java/crmeb/shell/payment_ops_cron.sh`
       - 新增参数：`--decision-chain-notify 0|1`、`--decision-chain-cron EXPR`
       - 支持定时执行 `payment_decision_chain_smoke.sh`
     - `crmeb_java/crmeb/shell/payment_ops_cron_smoke.sh`
       - 新增断言：托管块必须包含 `payment_decision_chain_smoke.sh`
     - `crmeb_java/crmeb/shell/payment_summary_contract_check.sh`
       - `go_nogo/warroom` 合同新增 date_match/date 字段校验
     - `crmeb_java/crmeb/shell/payment_summary_contract_smoke.sh`
       - 夹具补齐 `go_nogo/warroom` 新字段
     - `crmeb_java/crmeb/shell/payment_ops_status_smoke.sh`
       - 夹具补齐 date_match 字段，保证 pass_case 稳定为 GREEN
     - `crmeb_java/crmeb/shell/payment_preflight_check.sh`
       - 增加 cron 检查项：`payment_decision_chain_smoke.sh` 是否纳管
   - 本地验证：
     - `./shell/payment_ops_status_smoke.sh`（rc=0，severity=PASS）
     - `./shell/payment_summary_contract_smoke.sh`（rc=0，severity=PASS）
     - `./shell/payment_decision_chain_smoke.sh`（rc=0，severity=PASS）
     - `./shell/payment_ops_cron_smoke.sh --no-alert`（rc=0，severity=PASS）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_go_nogo_decision.sh --date 2026-02-14 --require-booking-repair-pass 1 --no-alert`（rc=0）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_warroom_dashboard.sh --date 2026-02-14 --window-hours 72 --no-alert`（rc=0，overall=GREEN）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_ops_status.sh --refresh --require-booking-repair-pass 1 --no-alert`（rc=0，overall=GREEN）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_summary_contract_check.sh --date 2026-02-14 --no-alert`（rc=0，warn=0，fail=0）
     - `ALERT_WEBHOOK_TYPE='feishu' ALERT_WEBHOOK_URL='***' ./shell/payment_ops_cron.sh --apply --mysql-defaults-file /root/.my.cnf --report-notify 1 --ticket-notify 1 --warroom-notify 1 --go-nogo-notify 1 --go-nogo-require-booking-repair-pass 1 --morning-bundle-notify 1 --morning-bundle-require-booking-repair-pass 1 --status-notify 1 --status-refresh 1 --status-require-booking-repair-pass 1 --contract-notify 1 --sla-notify 1 --retention-notify 1 --booking-repair-notify 1 --booking-repair-apply 1 --decision-chain-notify 1`（rc=0）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_preflight_check.sh`（PASS=28，WARN=4，FAIL=0）
50. D34 判定链路门槛化 + 日报/看板可见性（新增）
   - 背景：
     - D33 完成“日期一致性硬告警”，但 `ops_status` 未提供独立 decision-chain 门槛，且日报/看板未完整展示该指标。
   - 脚本增强：
     - `crmeb_java/crmeb/shell/payment_ops_status.sh`
       - 新增参数：`--require-decision-chain-pass 0|1`（默认 0）
       - 聚合 `latest decision_chain_smoke`，输出 `decision_chain_summary/decision_chain_severity/decision_chain_fail_count`
       - 开启门槛后：缺失或非 PASS 直接 block
       - `--refresh + 门槛开启` 时，主动执行一轮 `payment_decision_chain_smoke`，避免读旧产物
     - `crmeb_java/crmeb/shell/payment_ops_cron.sh`
       - 新增参数：`--status-require-decision-chain-pass 0|1`
       - `status` 任务自动透传 `--require-decision-chain-pass`
       - `decision-chain` 默认 cron 调整为 `26 2 * * *`，确保先于 `status(28 2 * * *)` 执行
     - `crmeb_java/crmeb/shell/payment_ops_cron_smoke.sh`
       - 新增断言：`payment_ops_status.sh` 命令包含 `--require-decision-chain-pass 1`
     - `crmeb_java/crmeb/shell/payment_warroom_dashboard.sh`
       - 聚合 `latest_decision_chain_summary`
       - 输出 `decision_chain_severity/decision_chain_fail_count`
       - 当 cron 已启用 decision-chain 但 summary 缺失，或 severity 非 PASS，计入 `risk_count`
     - `crmeb_java/crmeb/shell/payment_daily_report.sh`
       - 新增“判定链路自测”分段，日报和群摘要可见 `decision_chain` 指标
     - `crmeb_java/crmeb/shell/payment_summary_contract_check.sh`
       - 新增 `decision_chain_smoke` 合同检查
       - `warroom` 合同新增 `latest_decision_chain_summary/decision_chain_*` 字段
     - `crmeb_java/crmeb/shell/payment_summary_contract_smoke.sh`
       - 夹具补齐 `decision_chain_smoke` 与 `warroom` 新字段
     - `crmeb_java/crmeb/shell/payment_preflight_check.sh`
       - 新增检查项：`cron值守任务` 是否配置 `--require-decision-chain-pass`
   - 本地验证：
     - `./shell/payment_ops_status_smoke.sh`（rc=0，severity=PASS）
     - `./shell/payment_summary_contract_smoke.sh`（rc=0，severity=PASS）
     - `./shell/payment_decision_chain_smoke.sh`（rc=0，severity=PASS）
     - `./shell/payment_ops_cron_smoke.sh --no-alert`（rc=0，severity=PASS）
     - `ALERT_WEBHOOK_TYPE='feishu' ALERT_WEBHOOK_URL='***' ./shell/payment_ops_cron.sh --apply --mysql-defaults-file /root/.my.cnf ... --status-require-booking-repair-pass 1 --status-require-decision-chain-pass 1 --decision-chain-notify 1`（rc=0；托管块中 decision-chain 为 `26 2 * * *`）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_warroom_dashboard.sh --date 2026-02-14 --window-hours 72 --no-alert`（rc=0，overall=GREEN）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_ops_status.sh --refresh --require-booking-repair-pass 1 --require-decision-chain-pass 1 --no-alert`（rc=0，overall=GREEN）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_daily_report.sh --date 2026-02-14`（rc=0）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_summary_contract_check.sh --date 2026-02-14 --no-alert`（rc=0，warn=0，fail=0）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_preflight_check.sh`（PASS=29，WARN=4，FAIL=0）
51. D35 判定链路失败工单化（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_decision_ticketize.sh`
   - 能力：
     - 聚合 `ops_status/go_nogo/warroom/decision_chain_smoke` 最新同日产物
     - 自动生成 `tickets_decision.tsv`、`tickets_decision.md`、`summary_decision.txt`
     - P1 工单可直接触发告警（可 `--no-alert` 关闭）
   - 链路接入：
     - `payment_daily_report.sh` 新增 decision-ticket 摘要字段
     - `payment_ops_cron.sh` 新增 `--decision-ticket-notify/--decision-ticket-cron`
     - `payment_preflight_check.sh` 新增脚本存在性与 cron 纳管检查
   - 本地验证：
     - `./shell/payment_decision_ticketize.sh --date 2026-02-14 --no-alert`（rc=0，total=0，p1=0，p2=0）
     - `./shell/payment_ops_cron_smoke.sh --no-alert`（rc=0，包含 decision_ticketize 断言）
52. D36 值守离线自测补强（新增）
   - 增强脚本：
     - `crmeb_java/crmeb/shell/payment_ops_status_smoke.sh`
       - 新增 `decision_gate_case`：开启 `--require-decision-chain-pass=1` 且缺少 decision-chain summary 时应返回 `rc=2`
       - 修复报告模板反引号转义，避免 shell 将文案误执行为命令
   - 本地验证：
     - `./shell/payment_ops_status_smoke.sh`（rc=0，pass_case=0，stale_case=2，decision_gate_case=2）
53. D39 cron 自监控（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_cron_healthcheck.sh`
   - 能力：
     - 检查托管 cron 块是否存在
     - 检查关键 cron 日志（含 `decision_ticketize`）启用状态与新鲜度
     - 输出 `runtime/payment_cron_healthcheck/run-*/summary.txt + report.md`
   - 链路接入：
     - `payment_ops_cron.sh` 新增 `--cron-health-notify/--cron-health-cron`
     - `payment_preflight_check.sh` 新增脚本存在性与 cron 纳管检查
     - `payment_runtime_retention.sh` 纳管 `payment_cron_healthcheck`
   - 本地验证：
     - `./shell/payment_cron_healthcheck.sh --no-alert`（rc=0，overall=GREEN，warn=0，fail=0）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_preflight_check.sh`（PASS=33，WARN=4，FAIL=0）
     - `ALERT_WEBHOOK_TYPE='feishu' ALERT_WEBHOOK_URL='***' ./shell/payment_ops_cron.sh --apply ... --decision-ticket-notify 1 --decision-chain-notify 1 --cron-health-notify 1`（rc=0）
54. D40 日报近7天趋势（新增）
   - 增强脚本：
     - `crmeb_java/crmeb/shell/payment_daily_report.sh`
   - 新增能力：
     - 近7天趋势表（按报告日回看）：`unresolved_diff`、`ticket_p1`、`decision_chain_fail_count`
     - `decision_chain` 趋势按 `report_date` 精确匹配最新 summary，避免跨日串值
   - 本地验证：
     - `./shell/payment_daily_report.sh --date 2026-02-14`（rc=0）
     - 产物：`runtime/payment_daily_report/2026-02-14/payment_daily_report_2026-02-14.md` 已包含“九、近7天趋势”
55. D41 工单 owner 自动分配（新增）
   - 增强脚本：
     - `crmeb_java/crmeb/shell/payment_reconcile_ticketize.sh`
       - 新增参数：`--owner-map-file/--owner-default/--owner-p1`
       - `escalation.tsv/escalation.md` 的 owner 由规则引擎自动填充
     - `crmeb_java/crmeb/shell/payment_decision_ticketize.sh`
       - 新增参数：`--owner-map-file/--owner-default/--owner-p1`
       - `tickets_decision.tsv/md` 新增 owner 字段
     - `crmeb_java/crmeb/shell/payment_ops_cron.sh`
       - 新增参数：`--ticket-owner-map-file/--ticket-owner-default/--ticket-owner-p1`
       - 定时工单任务自动透传 owner 参数
     - `crmeb_java/crmeb/shell/payment_ops_cron_smoke.sh`
       - 新增断言：ticketize 命令行必须包含 owner 默认参数
   - 本地验证：
     - `./shell/payment_ops_cron_smoke.sh --no-alert`（rc=0，severity=PASS）
     - `./shell/payment_reconcile_ticketize.sh --date 2026-02-14 --owner-map-file /tmp/payment_owner_map.tsv --owner-default payment-ops --owner-p1 payment-oncall --no-alert`（rc=2，无差异工单）
     - `./shell/payment_decision_ticketize.sh --date 2026-02-14 --owner-map-file /tmp/payment_owner_map.tsv --owner-default payment-ops --owner-p1 payment-oncall --no-alert`（rc=0）
56. D42 门店映射审计脚本（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_store_mapping_audit.sh`
   - 能力：
     - 审计缺失映射、`sub_mchid` 格式异常、重复复用、孤儿配置
     - 输出 `summary/report/detail/missing/duplicate/orphan`
     - 支持 `--strict-missing 0|1`（缺失映射告警或阻断）
   - 链路接入：
     - `payment_preflight_check.sh` 纳管脚本存在性检查
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_store_mapping_audit.sh --strict-missing 0 --no-alert`（rc=2，overall=YELLOW，warn_count=2）
57. D43 无订单 mock 回放编排（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_mock_replay.sh`
   - 能力：
     - 串联 `ops_status_smoke/summary_contract_smoke/decision_chain_smoke/ops_cron_smoke/decision_ticketize/cron_healthcheck/store_mapping_audit`
     - 输出统一 `summary/report`，可在无订单号阶段日常回归
   - 链路接入：
     - `payment_preflight_check.sh` 纳管脚本存在性检查
     - `payment_runtime_retention.sh` 纳管 `payment_mock_replay` 目录
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_mock_replay.sh --date 2026-02-14 --owner-map-file /tmp/payment_owner_map.tsv --no-alert`（rc=2，overall=YELLOW，fail=0，warn=1）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_preflight_check.sh`（PASS=35，WARN=4，FAIL=0）
     - `./shell/payment_runtime_retention.sh --keep-days 7`（dry-run rc=0，新增目录扫描通过）
58. D44 切换上线拦截规则统一收敛（新增）
   - 新增脚本：
     - `crmeb_java/crmeb/shell/payment_cutover_gate.sh`
   - 能力：
     - 串联 `preflight + store_mapping_audit + mock_replay + go_nogo + ops_status`
     - 统一输出 `gate_decision=GO|GO_WITH_WARN|NO_GO` 与 blocker/warn 明细
     - 支持门槛参数：`--require-apply-ready/--require-booking-repair-pass/--require-mapping-green/--require-mock-green`
     - 支持 `--preflight-strict 0|1`（默认 0，离线阶段避免长期 NO_GO 噪声）
   - 链路接入：
     - `payment_preflight_check.sh` 纳管 `payment_cutover_gate.sh` 存在性检查
     - `payment_runtime_retention.sh` 纳管 `payment_cutover_gate` 目录
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_cutover_gate.sh --date 2026-02-14 --mapping-strict-missing 0 --require-mapping-green 0 --require-mock-green 0 --owner-default payment-ops --owner-p1 payment-oncall --no-alert`（rc=0，overall=YELLOW，gate_decision=GO_WITH_WARN，block=0，warn=1）
59. D45 门店映射审计 cron 纳管（新增）
   - 增强脚本：
     - `crmeb_java/crmeb/shell/payment_ops_cron.sh`
       - 新增参数：`--mapping-audit-notify/--mapping-audit-cron/--mapping-audit-strict-missing`
       - 任务日志：`runtime/payment_store_mapping_audit_cron.log`
     - `crmeb_java/crmeb/shell/payment_ops_cron_smoke.sh`
       - 新增断言：dry-run 输出包含 `payment_store_mapping_audit.sh --strict-missing`
     - `crmeb_java/crmeb/shell/payment_cron_healthcheck.sh`
       - 新增 `mapping_audit` 检查项（可选任务，未开启不判 FAIL）
     - `crmeb_java/crmeb/shell/payment_preflight_check.sh`
       - 新增 `cron映射审计任务` 检查项
   - 本地验证：
     - `./shell/payment_ops_cron_smoke.sh --no-alert`（rc=0，severity=PASS）
     - `./shell/payment_ops_cron.sh --mysql-defaults-file /root/.my.cnf --report-notify 1 --ticket-notify 1 --decision-ticket-notify 1 --warroom-notify 1 --go-nogo-notify 1 --morning-bundle-notify 1 --status-notify 1 --status-refresh 1 --contract-notify 1 --sla-notify 1 --retention-notify 1 --booking-repair-notify 1 --decision-chain-notify 1 --cron-health-notify 1 --mapping-audit-notify 1 --mapping-audit-strict-missing 0`（dry-run rc=0，命令块包含 mapping-audit 任务）
     - `./shell/payment_cron_healthcheck.sh --no-alert`（rc=0，overall=GREEN）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_preflight_check.sh`（PASS=36，WARN=5，FAIL=0）
60. D46 切换上线拦截规则 cron 纳管（新增）
   - 增强脚本：
     - `crmeb_java/crmeb/shell/payment_ops_cron.sh`
       - 新增参数：
         - `--cutover-gate-notify/--cutover-gate-cron`
         - `--cutover-gate-require-apply-ready/--cutover-gate-require-booking-repair-pass`
         - `--cutover-gate-mapping-strict-missing/--cutover-gate-require-mapping-green/--cutover-gate-require-mock-green`
         - `--cutover-gate-owner-map-file`
       - 任务日志：`runtime/payment_cutover_gate_cron.log`
     - `crmeb_java/crmeb/shell/payment_ops_cron_smoke.sh`
       - 新增断言：dry-run 输出包含 `payment_cutover_gate.sh` 与关键门槛参数
     - `crmeb_java/crmeb/shell/payment_cron_healthcheck.sh`
       - 新增 `cutover_gate` 检查项（可选任务，未开启不判 FAIL）
     - `crmeb_java/crmeb/shell/payment_preflight_check.sh`
       - 新增 `cron切换上线拦截规则任务` 检查项
   - 本地验证：
     - `./shell/payment_ops_cron_smoke.sh --no-alert`（rc=0，severity=PASS）
     - `./shell/payment_ops_cron.sh --mysql-defaults-file /root/.my.cnf --report-notify 1 --ticket-notify 1 --decision-ticket-notify 1 --warroom-notify 1 --go-nogo-notify 1 --morning-bundle-notify 1 --status-notify 1 --status-refresh 1 --contract-notify 1 --sla-notify 1 --retention-notify 1 --booking-repair-notify 1 --decision-chain-notify 1 --cron-health-notify 1 --mapping-audit-notify 1 --mapping-audit-strict-missing 0 --cutover-gate-notify 1 --cutover-gate-require-apply-ready 0 --cutover-gate-require-booking-repair-pass 1 --cutover-gate-mapping-strict-missing 0 --cutover-gate-require-mapping-green 0 --cutover-gate-require-mock-green 0`（dry-run rc=0，命令块包含 cutover-gate 任务）
     - `ALERT_WEBHOOK_TYPE='feishu' ALERT_WEBHOOK_URL='***' ./shell/payment_ops_cron.sh --apply --mysql-defaults-file /root/.my.cnf --report-notify 1 --ticket-notify 1 --decision-ticket-notify 1 --ticket-max-rows 200 --ticket-amount-p1-threshold-cent 100000 --warroom-notify 1 --go-nogo-notify 1 --go-nogo-require-booking-repair-pass 0 --morning-bundle-notify 1 --morning-bundle-require-booking-repair-pass 0 --status-notify 1 --status-refresh 1 --status-require-booking-repair-pass 0 --status-require-decision-chain-pass 1 --status-max-summary-age-minutes 240 --status-max-recon-age-days 2 --status-max-daily-report-age-days 2 --contract-notify 1 --sla-notify 1 --sla-lookback-days 14 --sla-days 1 --retention-notify 1 --booking-repair-notify 1 --booking-repair-window-hours 72 --booking-repair-apply 0 --decision-chain-notify 1 --cron-health-notify 1 --mapping-audit-notify 1 --mapping-audit-strict-missing 0 --cutover-gate-notify 1 --cutover-gate-require-apply-ready 0 --cutover-gate-require-booking-repair-pass 0 --cutover-gate-mapping-strict-missing 0 --cutover-gate-require-mapping-green 0 --cutover-gate-require-mock-green 0`（rc=0）
     - `./shell/payment_cron_healthcheck.sh --no-alert`（首次 rc=2，因新任务日志未生成；补跑 `mapping_audit/cutover_gate` 后 rc=0，overall=GREEN）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_preflight_check.sh`（PASS=38，WARN=4，FAIL=0）
61. D47 值守刷新补齐核销回归（新增）
   - 增强脚本：
     - `crmeb_java/crmeb/shell/payment_ops_status.sh`
       - `--refresh` 链路新增 `payment_booking_verify_regression.sh --window-hours 72 --no-alert`
       - 新增 summary 字段：`refresh_booking_verify_rc/refresh_booking_verify_summary`
       - 若刷新成功，优先使用刷新后的 `booking_verify_summary`，避免 stale 误告警
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_cutover_gate.sh --date 2026-02-14 --mapping-strict-missing 0 --require-mapping-green 0 --require-mock-green 0 --owner-default payment-ops --owner-p1 payment-oncall --no-alert`（rc=0；ops_status_rc=0，ops_status_overall=GREEN）
62. D48 上线拦截规则去重降噪（新增）
   - 增强脚本：
     - `crmeb_java/crmeb/shell/payment_mock_replay.sh`
       - 新增参数：`--skip-store-mapping-audit`
     - `crmeb_java/crmeb/shell/payment_cutover_gate.sh`
       - 当上线拦截规则已执行独立 `mapping_audit` 时，调用 `mock_replay` 自动追加 `--skip-store-mapping-audit`
   - 结果：
     - 上线拦截规则告警由“映射审计+mock重复告警”收敛为单一“映射审计告警”
   - 本地验证：
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_mock_replay.sh --date 2026-02-14 --skip-store-mapping-audit --owner-default payment-ops --owner-p1 payment-oncall --no-alert`（rc=0，overall=GREEN）
     - `MYSQL_DEFAULTS_FILE=/root/.my.cnf ./shell/payment_cutover_gate.sh --date 2026-02-14 --mapping-strict-missing 0 --require-mapping-green 0 --require-mock-green 0 --owner-default payment-ops --owner-p1 payment-oncall --no-alert`（rc=0，overall=YELLOW，warn=1）

## 当前状态
- D1: 已具备执行条件（等待真实服务商号后做全量联调）
- D2: 已完成代码级安全补强
- D3: 已落地日对账脚本并接入定时，凭据安全方案已切到 defaults-file
- D4: 已有映射脚本，待真实 `storeId -> sub_mchid` 台账
- D5: 已完成代码级状态机、幂等收口、超时补偿与对账联动
- D6: 已有巡检+告警+cron安装器并已 apply，飞书告警已恢复，含日报+ticketize+warroom+go_nogo+morning_bundle+status+contract+reconcile_sla+retention
- D7: 已有演练脚本+预检脚本并已接入定时，待首轮真实订单演练
- D23: 已将 ticket P1 纳入 go/no-go 硬门槛，并补齐 escalation 升级看板
- D24: 已打通预约核销与会员卡扣减留痕，履约闭环可独立推进
- D25: 已把预约核销一致性纳入回归检查，并接入早班一键包日常巡检链路
- D26: 已补齐预约核销异常自动修复脚本，支持 dry-run/apply 修复非资金字段
- D27: 已把核销修复接入 morning/warroom/status/daily 主链路，并支持 go/no-go 可选“repair必须PASS”门槛
- D28: 已支持 booking-repair 定时任务可切换 dry-run/apply，并可在 status 层启用“repair必须PASS”门槛
- D29: 已按阶段策略收敛 apply_summary 过期噪音，严格值守可稳定回到 GREEN
- D30: 已修复值守汇总跨日期误读风险，状态判定稳定性进一步提升
- D31: 已修复 go_nogo/warroom 判定链路跨日期误读风险，日切稳定性进一步提升
- D32: 已补判定链路离线专项回归，并接入 preflight/retention，后续改动可持续防回归
- D33: 已把日期一致性纳入硬告警与总览阻断，并完成 cron 纳管离线自测
- D34: 已完成 decision-chain 门槛化、看板与日报可见性，严格值守默认链路继续保持 GREEN
- D35: 已补齐判定链路工单化脚本并接入 cron，可自动产出决策类 P1/P2 工单
- D36: 已补 decision-chain 门槛离线回归分支并修复报告渲染缺陷
- D39: 已补 cron 自监控并纳管到 cron/preflight/retention，当前健康检查为 GREEN
- D40: 已补日报近7天趋势（unresolved_diff/ticket_p1/decision_chain_fail_count），可用于晨会看风险走势
- D41: 已支持工单 owner 自动分配（规则文件 + 默认值班 owner），cron 已可透传
- D42: 已补门店映射审计脚本，可持续识别缺失/重复/格式/孤儿配置风险
- D43: 已补无订单 mock 回放编排，离线阶段可每日自动回归主链路
- D44: 已补切换上线拦截规则统一收敛脚本，切换前可一键得到 GO/NO_GO 结论
- D45: 已把门店映射审计纳入 cron 编排与健康检查，映射风险可日常自动暴露
- D46: 已把切换上线拦截规则纳入 cron 编排与健康检查，可按日自动产出 GO/NO_GO 结论
- D47: 已把 booking_verify_regression 纳入 ops_status refresh，值守 stale 告警显著收敛
- D48: 已完成上线拦截规则链路去重降噪，离线阶段切换上线拦截规则只保留真实映射风险告警

## 待你提供后继续（外部依赖）
1. 服务商号 `sp_mchid`（预计 3月底/4月中旬）
2. 门店子商户台账 `storeId -> sub_mchid`
3. 生产环境 APIv2 key / 证书路径（仅服务器配置，不入库明文）

## 下一步（我可直接继续）
1. 用真实订单执行 `payment_fullchain_drill.sh --order-no <订单号>`（验证退款链路与对账一致性）
2. 提供 `storeId -> sub_mchid` 正式台账并执行 `payment_store_mapping_import.sh --apply`
3. 服务商号到位后执行 `payment_service_provider_fill.sh --apply` 完成首轮联调
