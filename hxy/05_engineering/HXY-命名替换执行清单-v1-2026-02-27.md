# HXY 命名替换执行清单 v1（2026-02-27）

## 1. 已完成（本次已落地）

1. 运行库名切换为 `hxy_dev`（原 `ruoyi-vue-pro` 保留为回滚库）。
2. Docker 运行参数默认库名切换为 `hxy_dev`：
   - `ruoyi-vue-pro-master/script/docker/docker.env`
   - `ruoyi-vue-pro-master/script/docker/docker-compose.yml`
3. 管理后台标题改为 `荷小悦运营平台`（不再展示芋道品牌）。
4. 后端默认禁用提示文案去掉 `yudao-module-*` 字样：
   - `ruoyi-vue-pro-master/yudao-server/src/main/java/cn/iocoder/yudao/server/controller/DefaultController.java`
5. 支付联调脚本默认数据库改为 `hxy_dev`（避免继续引用旧库名）：
   - `ruoyi-vue-pro-master/script/dev/*.sh`（涉及 `DB_NAME` 默认值的脚本）
6. 第二阶段启动：容器名/镜像名/CI 名统一进入 `hxy-*` 轨道（详见 `HXY-第二阶段命名迁移启动-v1-2026-02-28.md`）。

## 2. 本次修复的配套数据库问题

1. 修复商城模块租户列缺失（`tenant_id`）导致的 500：
   - `ruoyi-vue-pro-master/sql/mysql/hxy/2026-02-27-hxy-mall-tenant-id-fix.sql`
2. 修复商品创建表结构不兼容（`product_spu/product_sku`）：
   - `ruoyi-vue-pro-master/sql/mysql/hxy/2026-02-27-hxy-product-spu-schema-compat-fix.sql`

## 3. 暂未做（需分阶段迁移，不建议一次性硬改）

1. Java 包名 `cn.iocoder.yudao.*` 全量替换为 `com.hxy.*`。
2. Maven 坐标（`groupId/artifactId`）全量替换。
3. 仓库主目录 `ruoyi-vue-pro-master` 物理重命名。
4. 容器名/镜像名全量去 `yudao-*`（会影响现有脚本与运维依赖，需单独窗口切换）。

## 4. 下一阶段建议

1. 先做“新增资产门禁”：禁止新增 `yudao/ruoyi/crmeb` 命名。
   - 已提供脚本：`ruoyi-vue-pro-master/script/dev/check_hxy_naming_guard.sh`
   - 词表配置：`ruoyi-vue-pro-master/script/dev/config/hxy_naming_forbidden_words.txt`
2. 再做“脚本与运维名迁移”：容器、镜像、CI 任务名统一 `hxy-*`。
3. 最后做“代码包名迁移”：按模块批次迁移并逐模块回归。
